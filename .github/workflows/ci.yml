name: Repository CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  quality-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Architecture check
        run: npm run check:architecture

      - name: Lint
        run: npm run lint

      - name: Knip (dead code detection)
        run: |
          node --version
          npm --version
          ls -la node_modules/.bin/knip || true
          ls -la node_modules/knip/bin/ || true
          node --version
          node ./node_modules/knip/bin/knip.js --version || echo "knip --version exit: $?"
          npm run check:knip

      - name: Compile
        run: npm run compile
