# Temporary oversized source-file debt allowlist.
# Scope:
# - only handwritten source files under src/ and packages/**/src/
# - generated outputs (dist/build/node_modules) are excluded by directory, not by this file
#
# Contract:
# - every path here is existing architecture debt
# - new >500-line files must not be added here casually
# - remove entries as soon as the file drops to 500 lines or below

# Application Skeleton Phase B orchestration pilot: typed acceptance fallback
# routing pushed `session-request-handler-message-dispatch.ts` four lines over
# the 500-line limit. The dispatch class is the canonical entry point for
# every provider message and a deeper refactor is out of scope for the pilot.
# Remove this entry once the dispatch class is sliced into smaller modules.
packages/core/src/remote-bridge/handlers/session-request-handler-message-dispatch.ts

# Application Skeleton acceptance phrase recognizer hot-fix (Phase 20): adding
# the 200-character length cap + explanatory comment pushed
# `managed-workflow-post-turn-service.ts` one line over the 500-line limit.
# The post-turn service hosts the central arbitration pipeline; a deeper slice
# refactor is out of scope for the hot-fix. Remove this entry once the post-turn
# service is sliced into focused per-stage modules.
packages/core/src/remote-bridge/handlers/managed-workflow-post-turn-service.ts
