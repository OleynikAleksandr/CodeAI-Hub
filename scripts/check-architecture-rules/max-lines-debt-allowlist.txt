# Temporary oversized source-file debt allowlist.
# Scope:
# - only handwritten source files under src/ and packages/**/src/
# - generated outputs (dist/build/node_modules) are excluded by directory, not by this file
#
# Contract:
# - every path here is existing architecture debt
# - new >500-line files must not be added here casually
# - remove entries as soon as the file drops to 500 lines or below

# Provider message dispatch rewrite debt: the canonical dispatch entrypoint
# still contains retry/session-boundary branches and the temporary technical
# stage rewrite blocker. Remove this entry once those branches are sliced into
# smaller modules.
packages/core/src/remote-bridge/handlers/session-request-handler-message-dispatch.ts

# Settings handler debt: pre-existing ~498-line file pushed to ~514 by the
# Kimi thinkingEnabled force-restart wiring (constructor deps + handleSave
# hook). Remove once the Apple Native preflight block is extracted into its
# own module.
packages/core/src/remote-bridge/handlers/settings-request-handler.ts
