# Архитектура Унифицированной Системы Сессий

**Дата:** 3 ноября 2025  
**Статус:** Design Approved

---

## Проблема

Три AI провайдера (Claude, Codex, Gemini) имеют разные форматы:
- SDK feedback (real-time stream)
- Локальные JSONL файлы чатов

Это создаёт сложности:
- Два разных парсера (real-time vs stored)
- Невозможность refresh/resume с единым форматом
- Отсутствие поддержки multi-provider чатов

---

## Решение: Двухслойная система

### Слой 1: Провайдерская система (их территория)

**Claude:**
```
~/.claude/projects/{workspace_path}/{sessionId}.jsonl
Пример: ~/.claude/projects/-Users-oleksandroliinyk-VSCODE-CodeAI-Hub/abc123.jsonl
```

**Codex:**
```
~/.codex/sessions/{YYYY}/{MM}/{DD}/{session-timestamp-uuid}.jsonl
Пример: ~/.codex/sessions/2025/11/03/rollout-2025-11-03T08-35-11-019a48a4.jsonl
```

**Gemini:**
```
~/.gemini/tmp/{hash}/chats/session-{timestamp-shortid}.json
Пример: ~/.gemini/tmp/5dd7427e0941d284/chats/session-2025-11-03T10-22-130144b7.json
Примечание: hash непонятный, в имени файла только первые 8 символов sessionId
```

**Назначение:** Провайдеры используют для context continuation
**Наши действия:** НЕ читаем, НЕ пишем, НЕ трогаем

### Слой 2: CodeAI Hub система (наша территория)

**Структура каталогов:**
```
~/.codeai-hub/sessions/{workspace-slug}/
├── claude/{sessionId}.jsonl
├── codex/{sessionId}.jsonl
├── gemini/{sessionId}.jsonl
└── combined/
    ├── claude-codex/      (будущее)
    ├── claude-gemini/     (будущее)
    ├── codex-gemini/      (будущее)
    └── all/               (будущее - все 3 провайдера)
```

**Примеры:**
```
~/.codeai-hub/sessions/-Users-oleksandroliinyk-VSCODE-CodeAI-Hub/claude/abc123.jsonl
~/.codeai-hub/sessions/-Users-oleksandroliinyk-VSCODE-CodeAI-Hub/codex/xyz789.jsonl
~/.codeai-hub/sessions/-Users-oleksandroliinyk-VSCODE-CodeAI-Hub/gemini/def456.jsonl
```

**Назначение:** UI отображение (real-time, refresh, resume)
**Формат:** Унифицированный JSONL (см. раздел «Формат JSONL» ниже)
**Наши действия:** Пишем через wrapper, читаем одним парсером

> Папка `{workspace-slug}` — это нормализованный путь рабочей директории (тот же, что используется при генерации `claudeProjectSlug`). Она гарантирует, что разные проекты не смешают артефакты между собой даже при одновременном запуске.

---

## Data Flow

### Real-time сообщение:

```
User Request
    ↓
Provider SDK Stream (их формат)
    ↓
ProviderWrapper.normalize() → UnifiedMessage
    ↓
    ├─→ UnifiedSessionStorage.append(workspace) → наш JSONL
    └─→ UI.display() → показываем в интерфейсе
```

### Resume сценарий:

```
Провайдер                    CodeAI Hub UI
    ↓                              ↓
Читает свой JSONL          Читает наш JSONL
(свой формат)              (unified формат)
    ↓                              ↓
Загружает context          Отображает историю
    ↓                              ↓
Готов к продолжению        Показали чат в UI
```

### Refresh / Core snapshot

- Core больше **не** отдаёт историю сообщений через `/api/v1/status` и начальный `core:state` snapshot. Этот эндпоинт оставляет только метаданные сессий (идентификаторы, провайдеры, статус биндинга).
- UI при рефреше использует unified JSONL, а не оперативную память core. До тех пор, пока враперы не готовы, диалог панели получают историю только из live-потока (возможно пустого) и не восстанавливают её из старых SDK файлов.
- Core хранит у себя лишь минимальный контекст для управления lifecycle (binding, статус, тайминги), что позволяет безболезненно заменить внутреннее представление после внедрения унифицированного writer'а.

### Ключевая синхронизация:

- На старте ядро выдаёт временный идентификатор, чтобы роутить события. Как только SDK сообщает реальный `providerSessionId`, временный ID во всех внутренних структурах и в путях записи заменяется на подтверждённый.
- Провайдер управляет своим контекстом
- Мы управляем UI отображением
- Полная независимость = нет конфликтов

### Жизненный цикл записи

- Файл создаётся только после получения подтверждённого `providerSessionId`; первая строка — мета-событие `session-open`.
- Все события добавляются через единый writer, работающий синхронно и соблюдающий порядок доставки (UI и файл получают один и тот же поток).
- Завершение сессии фиксируется записью `session-close`, после чего writer освобождает ресурсы; повторное открытие той же сессии в UI воспроизводит файл целиком или инкрементально, если известен смещённый хвост.
- Политика хранения/очистки будет сформулирована отдельно; по умолчанию файлы остаются на диске, пока пользователь или обслуживающий сценарий явно их не удалит.
- Refresh ядра не выполняет дополнительного чтения: core опирается только на события, которые уже прошли через врапер. UI инициирует повторное отображение, перечитывая unified JSONL напрямую (через API/reader, которые появятся на следующем этапе).
- Каждая запись содержит только те поля, которые необходимы UI: `timestamp`, `role`, `content`, опциональные `metadata` для thinking/tool событий и идентификатор провайдера. Сырые SDK payload'ы и внутренние детали провайдера не сохраняются, чтобы не тянуть в JSONL лишние структуры.

---

## Подход к реализации

### Последовательное изучение и построение

Мы НЕ будем сразу проектировать все компоненты.

**Правильный подход:**

1. **Изучение SDK вывода каждого провайдера** - последовательно
   - Читаем real-time SDK stream для каждого типа сообщений
   - Документируем структуру данных
   - Выявляем общие паттерны и различия

2. **Построение wrapper'а** - по мере изучения
   - Для каждого провайдера отдельно
   - Нормализуем типы сообщений один за другим
   - Тестируем на реальных данных

3. **Итеративная разработка формата**
   - Unified формат будет эволюционировать
   - По мере изучения добавляем необходимые поля
   - Версионирование формата (см. раздел «Формат JSONL»)

### Компоненты (общее представление)

Основные блоки, которые появятся в процессе:
- **ProviderWrapper** - для каждого провайдера (нормализация SDK → Unified)
- **SessionStorage** - запись/чтение наших JSONL файлов
- **SessionManager** - управление lifecycle сессий
- **UnifiedFormat** - интерфейсы и типы (детали описаны в разделе «Формат JSONL»)

Детальная архитектура компонентов будет описана отдельно после изучения SDK.

---

## Преимущества архитектуры

✅ **Один парсер** - unified format для всех провайдеров  
✅ **Простота refresh/resume** - единый источник данных для UI  
✅ **Multi-provider ready** - поддержка N провайдеров в одном чате  
✅ **Полная независимость** - провайдеры не знают о нас  
✅ **Синхронность** - SessionID связывает обе системы  
✅ **Нет конфликтов** - каждый работает со своими файлами

---

## Следующие шаги

### Фаза 1: Изучение SDK вывода

1. **Claude SDK** - анализ real-time stream
   - Изучить все типы сообщений (user, assistant, tool, system, meta)
   - Документировать структуру каждого типа
   - Записать примеры в отдельный документ

2. **Codex SDK** - анализ real-time stream
   - То же самое для Codex
   - Сравнить с Claude, выявить различия

3. **Gemini SDK** - анализ real-time stream
   - То же самое для Gemini
   - Сравнить с Claude и Codex

### Фаза 2: Проектирование унифицированного формата

4. На основе изученных данных спроектировать окончательную структуру UnifiedMessage
5. Описать mapping: SDK формат → Unified формат для каждого провайдера
6. Определить стратегию версионирования

### Фаза 3: Реализация (детали будут уточнены)

7. Реализовать wrapper'ы последовательно
8. Интегрировать с UI
9. Тестирование: real-time → refresh → resume

**Детальный план будет составлен после завершения Фазы 1.**
