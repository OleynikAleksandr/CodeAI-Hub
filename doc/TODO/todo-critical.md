# План критических исправлений (P0)

## Автоостановка и владение портом
- [TODO] Вернуть детерминированное владение: расширение VS Code (владелец установки) при активации проверяет `/api/v1/health`, корректно гасит устаревшие копии через `/shutdown` и только затем перезапускает; лаунчер уважает lock/health и никогда не убивает текущего владельца (нужно обновить поток рестарта и автозапуск после выхода).  
  - Коммит: обязателен.
- [TODO] Если `CORE_PORT` занят, определять, наш ли это core (та же версия → переиспользуем, другая версия → показываем предупреждение и инициируем упорядоченный shutdown, чужой процесс → выбираем следующий свободный порт, фиксируем в runtime registry и рассылаем обоим клиентам) вместо бездумного убийства любого процесса на 8080.  
  - Коммит: обязателен.

## Изоляция провайдеров
- [TODO] Любая ошибка Claude/Gemini CLI должна помечать провайдера в состоянии `failed/degraded`, но не завершать orchestrator; UI показывает предупреждение, остальные провайдеры продолжают работать (поведение не подтверждено тестами, задача открыта).  
  - Коммит: обязателен.

## Регрессия в Unified Session
- [TODO] Проследить slug-путь (ENV → runtime registry → UnifiedSessionStorage) и убедиться, что JSONL пишутся в `~/.codeai-hub/sessions/-Users-oleksandroliinyk-VSCODE-CodeAI-Hub/` у обоих клиентов (предыдущая проверка оказалась недостоверной).  
  - Коммит: обязателен.
- [TODO] После исправлений перезапустить Extension и лаунчер, создать новые сессии и задокументировать фактические пути (повторить после реальных фиксов).  
  - Коммит: обязателен.
