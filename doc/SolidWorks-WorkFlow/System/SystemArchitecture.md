# System Architecture (SSOT) — SolidWorks-WorkFlow

**Scope:** каноническое описание системы CodeAI Hub целиком (уровень System).

## 0) Session role

Этот документ читается в начале сессии как базовый SSOT знакомства с системой:

- он объясняет глобальные инварианты;
- он показывает карту подсистем;
- он не является каталогом всех документов, которые нужно читать дальше.

Если после обсуждения новой задачи нужно выбрать дополнительные документы по конкретному scope, агент обязан использовать `doc/SolidWorks-WorkFlow/Docs_Index.md` и читать только релевантные документы, а не весь каталог подряд.

## 1) Компоненты системы (верхний уровень)

- **Core Orchestrator** (Node.js сервис): бизнес‑логика, turn lifecycle, routing, continuity.
- **Core Supervisor**: управление runtime, запуск/перезапуск и version attach.
- **Project Manager (CEF UI bundle)**: Workflow Tree + Sessions/Artifacts + stage/session routing UX + единственный живой Settings UI; PM также владеет runtime bootstrap authority для пользовательского запуска Core.
- **UI bundles**: `project-manager`, `vscode-webview` (extension-side settings surface = compat-only notice, не живой product UI).
- **VS Code extension shell**: distribution/install/bootstrap-components shell; не владеет runtime startup/attach и не является живым Settings owner.
- **CEF Launcher**: локальный клиент для Project Manager.
- **Providers**: Claude/Codex/Gemini модули (CLI/SDK контуры).
- **Shared runtime translation module**: `doc/SolidWorks-WorkFlow/Modules/Shared_RuntimeTranslation_Module.md` (package: `packages/translation/`; engine-neutral facade with engine-aware safe chunk planning and per-chunk fallback assembly for live overlays today and localization adapters tomorrow).
- **Localization module**: `doc/SolidWorks-WorkFlow/Modules/Localization.md` (package: `packages/localization/`; owns bundled English source catalogs, glossary protection, localized bundle persistence, runtime payload contracts, and UI lookup primitives).
- **Gemini bundled runtime dependency**: installed Gemini provider bundles vendor `@codeai-hub/translation` into their own runtime root so the provider can resolve the shared translation package outside the workspace `node_modules` tree.

## 2) SSOT уровни (иерархия документов)

- System SSOT (этот файл): глобальные инварианты и карта.
- Cluster SSOT: `doc/SolidWorks-WorkFlow/Clusters/*`.
- Module SSOT: `doc/SolidWorks-WorkFlow/Modules/*`.
- Contract SSOT: `doc/SolidWorks-WorkFlow/Contracts/*` (точечные механизмы).

## 3) Глобальные инварианты (must-not-break)

1. **Snapshot-first lock contract**: состояние input определяется только snapshot‑сигналами (`turnState`, continuity lock flags и т.п.). Reopened workflow dialog обязан иметь runtime session в `workspace:snapshot` после первого `dialog:list`: Core materialize-ит stub runtime session для каждой continuity entry (`registerSessionWithId` + `registerRestoredBinding` + `notifySessionCreated` с `turnState: "idle"`, `continuityLockActive: false`, `bindingStatus: "ready"`) без запуска provider adapter turn; PM не имеет права держать fallback-ветку для "runtime session отсутствует в snapshot". **Stale-binding auto-recovery (1.2.42):** `providerSessionStatus: "ready"` выставленный materializer-ом означает только "журналированная запись в ядре есть", но НЕ означает "provider-модуль уже держит активное досье на сессию". Первый user message после Core restart попадёт в `adapter.sendMessage` с `providerSessionId`, которого модульный state (Claude SDK Manager in-memory Map / Codex app-server handshakedThreadIds Set / Gemini session map) не знает. Каждый provider adapter обязан бросать typed stale-binding error с `code` (`CLAUDE_SESSION_STALE_BINDING` / `CODEX_SESSION_STALE_BINDING` / `GEMINI_SESSION_STALE_BINDING`) и `providerSessionId`; `SessionRequestHandlerMessageDispatch.dispatchUserMessage` распознаёт code и выполняет one-shot `invalidateProviderBinding` + `ensureSessionReadyForSend` + resend. Generic `Error` на этом пути недопустим — он приводит к silent drop (classifier помечает `retryable`, но retry не запускается). **Post-rebind usage_limits refresh (1.2.43):** retry-ветка после успешного `ensureSessionReadyForSend + resend` обязана одноразово триггерить `adapter.refreshUsageLimits` для свеже-hydrated session через `triggerPostRebindUsageLimitsRefresh`. PM отправляет `binding_ready` usage_limits refresh ровно один раз на логическую session, и тот первый refresh гонится против paper-binding до hydration — без повторного trigger'а usage_limits виджет остаётся пустым у Claude и Codex после Core restart. Gemini не страдает потому что делает proactive refresh внутри `startManagedSession`, остальные провайдеры полагаются на Core-триггер. **Codex PATH augmentation (1.2.43):** `CodexAppServerProcess.startInternal` расширяет inherited `process.env.PATH` curated-списком user-level install locations (`~/.npm-global/bin`, `/opt/homebrew/bin`, `/usr/local/bin`, `/usr/bin` на POSIX; `%APPDATA%\npm` на Windows) — без hardcoded absolute paths, PATH lookup остаётся primary mechanism. Это убирает `spawn codex ENOENT` при recovery re-spawn на macOS, где VS Code extension host inherit'ит GUI env без shell PATH extensions. **Single-probe usage_limits warmup (1.2.44):** `binding_ready` triggers для каждого reopened dialog ранее дёргали `adapter.refreshUsageLimits` каждый раз, что приводило к lost payload'ам на первом (до hydration) refresh и лишнему трафику на остальных. Теперь `SessionRequestHandler` держит account-wide `UsageLimitsWarmupTracker: Set<providerId>`: первый успешный `binding_ready` probe per provider per Core process запускает dispatch и помечает provider как warmed, все последующие `binding_ready` для того же провайдера делают только cached replay. Empty probe без payload не имеет права warming-ить provider и не должен suppress-ить следующий `binding_ready`. Другие lifecycle triggers (`turn_completed`, `reconnect`, `manual`, `provider_session_rebound`, `dialog_opened`, `session_opened`) не дедуплицируются — они отражают реальные state changes. Кэш уже account-scoped (`providerScopeKey = {providerId}:global` с `070139b9e`), поэтому один успешный probe наполняет payload для всех sessions провайдера одновременно. **Pre-turn reopened-dialog usage refresh (1.2.45/1.2.47):** PM держит provider-scoped usage cache и seed-ит из него новые dialog/runtime snapshots даже если source `sessionId` ещё не materialize-ился локально; при фактическом `dialog_opened` Core обязан сначала replay-ить cached usage snapshot, а затем запускать cheap provider refresh даже если cached payload уже есть. `adapter.refreshUsageLimits` на этом пути обязан быть awaitable и завершаться только после фактического provider read/broadcast, иначе Core lifecycle incorrectly concludes before payload exists. Поздний provider-scoped `usage_limits` payload обязан применяться ко всем matching provider-family snapshots даже если исходный `payload.sessionId` уже исчез локально из-за placeholder→restored runtime swap. Это обеспечивает last-known limits immediately + fresh limits before the next user turn without eager `resumeSession` / provider hydration.
   - Канон: `WorkspaceRuntime.md`, `SessionUI_Behavior.md`, `SessionInputLock_SSOT_StateMachine.md` §3.3, `CoreOrchestrator.md`, `packages/Claude_Module/src/provider/claude-session-stale-binding-error.ts`, `packages/Codex_AppServer_Module/src/provider/codex-session-stale-binding-error.ts`, `packages/Gemini_Module/src/provider/gemini-session-stale-binding-error.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-message-dispatch.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-post-rebind-usage-limits.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-usage-limits-warmup.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-usage-limits-refresh.ts`, `packages/Codex_AppServer_Module/src/app-server/process/codex-app-server-process.ts`.
2. **Dialogs vs status split**: история/диалог (`dialogId`) независим от live status/usage (`sessionId`); routing обязателен после restart/reconnect.
   - Канон: `Dialogs_And_Continuity_Routing.md`.
3. **Session continuity**: rollover/handoff обязаны быть надёжны и не залипать UI в working.
   - Канон: `SessionContinuity.md`.
   - Threshold-driven continuity для flow/document nodes разрешён только на post-turn boundary: `token_usage` не является сигналом завершения turn-а и не может немедленно прерывать активный one-shot turn.
4. **Workflow navigation SSOT**: любой route в workflow stage (sidebar tree click/branch-node click/auto-select/dialog-intent) обязан синхронизировать `activeStage`; выбранный sidebar node, открытая session и header правой панели не могут расходиться. Верхний stage toolbar удалён в v1.1.924.
   - Канон: `ProjectManager_WorkflowNavigation_SSOT.md`.
5. **Provider-home isolation**: provider state изолирован под `~/.codeai-hub/providers/<id>/home` (где применимо), без смешения с терминальным HOME. Для Claude этого уже недостаточно как общей формулы: CodeAI Hub-managed turns также обязаны идти в full SDK isolation mode без filesystem `CLAUDE.md` / settings discovery, иначе parent-chain от `cwd` может затащить personal memory из real home.
   - Канон: provider docs в `doc/SolidWorks-WorkFlow/Modules/*`.
6. **Response-mode diagnostics split**: shaping live Codex turn-ов (`strict` / `hybrid` / `debug_raw`) не может быть единственным местом, где существует provider output; raw provider logs остаются диагностическим SSOT до любых UI/history фильтров.
   - Канон: `doc/SolidWorks-WorkFlow/Contracts/Codex_ResponseMode_Settings_Architecture.md`, `doc/SolidWorks-WorkFlow/Modules/Codex.md`.
7. **Provider dialog segment preservation**: если provider runtime фактически отдает несколько assistant-replies внутри одного пользовательского turn-а, provider normalization layer не имеет права схлопывать их в один post-factum blob; допустим только fallback aggregate-path, когда streamed segment boundaries не были отданы вообще.
   - Gemini extension (1.2.9): provider-side quirks, которые лепят thought-like content в обычный `content`-поток без сопровождающих `ptype: "thought"` events, обязаны быть распознаны и вынесены в thinking overlay, а не прокинуты в финальный assistant bubble. Это покрывает два случая: (a) inline `[Thought: true]` / `[Thought: false]` разделитель внутри одного content stream между thought-like английским summary и финальным target-language ответом; (b) pre-tool английский progress-text на старте сессии, когда target `messagesForTheUserLanguage` в Cyrillic-family (`ru`/`uk`/`bg`/`sr`/`mk`/`be`/`ky`/`kk`/`mn`/`tg`/`ab`) и snapshot не содержит ни одного U+0400..U+052F символа. Литеральный `[Thought: ...]` token никогда не попадает в dialog.
   - Gemini extension (1.2.12): на `thinkingLevel=high` с большими Description Agent промптами Gemini 3.1 Pro Preview иногда эмитит свой internal meta-prompt (`sthought`, `CRITICAL INSTRUCTION`, `Related tools:`, `Plan:`, `Drafting the content`) через `Content` events вместо `Thought` events. `GeminiAssistantEventNormalizer.handleFinishedEvent` проверяет финальный assistantSegment через `hasMisroutedThinkingPrefix` (после 1.2.9 marker-splitter и pre-tool heuristic) и маршрутизирует весь segment через `emitInlineThoughtAsThinking` если сработал префикс. Это UI-correctness patch до upstream-fix от Google; список префиксов в `MISROUTED_THINKING_PREFIXES` — это allowlist, расширяется консервативно по наблюдениям retest'ов.
   - Канон: `doc/SolidWorks-WorkFlow/Modules/Gemini.md`.
8. **Workspace-scoped stream replay**: stateful session signals (`token_usage`, `usage_limits`), которые могут прийти до attach/rebind workspace scope, обязаны иметь replay-safe transport path после websocket connect и после смены scope; single-shot delivery для таких сигналов недопустим.
   - Канон: `doc/SolidWorks-WorkFlow/Contracts/SessionUI_Behavior.md`, `doc/SolidWorks-WorkFlow/Contracts/WorkspaceRuntime.md`.
9. **Provider-order-safe continuity arbitration**: Core обязан одинаково корректно обрабатывать оба event order-а (`token_usage -> turn_completed` и `turn_completed -> token_usage`); trailing usage может завершать уже начатую post-turn arbitration, а cached usage обязан быть turn-scoped и очищаться после решения.
   - Explicit unavailable fallback (1.2.16): если eligible flow-node session завершилась `turn_completed`, но provider знает, что trailing usage snapshot для этого turn-а уже не появится, provider обязан эмитить explicit `postTurnTokenUsageUnavailable: true`. Только этот explicit signal разрешает Core завершить pending post-turn arbitration как `no_rollover`; просто отсутствие usage не может молча unlock-нуть session.
   - Канон: `doc/SolidWorks-WorkFlow/Contracts/SessionContinuity.md`, `doc/SolidWorks-WorkFlow/Clusters/CoreOrchestrator.md`.
10. **Provider failure classification before teardown**: Core обязан классифицировать provider error через `ProviderFailureClassifier` до удаления binding. Transient errors (`transient_turn_failure`) не должны удалять binding, деградировать whole provider или молча дропать user message. Retry budget ограничен (1 transient + 1 auto-resume), pending user intent имеет TTL=60s.
   - Канон: `doc/SolidWorks-WorkFlow/Contracts/ProviderFailure_Recovery_And_ProviderSwitch.md`.
11. **Provider-neutral switch transfer**: cross-provider takeover обязан использовать `unified-dialog.prompt.md` (plain `User:/Assistant:` transcript) и `provider-switch-handoff.md`, а не provider-native JSONL/rollout/SDK logs. `dialog:switch:*` protocol является единственным bridge contract для recovery и manual switch.
   - Канон: `doc/SolidWorks-WorkFlow/Contracts/ProviderFailure_Recovery_And_ProviderSwitch.md`.
12. **Quality-gate contract is workflow-critical**: локальный `pre-commit` обязан прогонять architecture gate, repo-wide `npm run lint`, `npm run check:tsprune` и staged-only formatting. Хук не имеет права форматировать весь репозиторий поверх незастейдженных изменений.
   - Канон: `.husky/pre-commit`, `doc/TODO/todo-plan.md`.
13. **Release package truthfulness**: VSIX/package surface не должен тащить repo-only workflow files и hook helpers (`.husky/**`, `.gitignore`, `GEMINI.md`, `AGENTS.md`, `doc/`, `scripts/`), если runtime не использует их напрямую.
   - Канон: `.vscodeignore`, `README.md`, `CHANGELOG.md`.
14. **Effective model identity SSOT**: `modelId` в transport/runtime/UI контракте означает полную effective model identity, а не только base model; reasoning/thinking являются частью identity, и единственным source of truth для next-turn model identity является `~/.codeai-hub/settings/settings.json`. Canonical runtime settings path is hard-cut to this file: launcher/Core bootstrap and extension-side settings storage are not allowed to fall back to `~/.codeai-hub/settings/claude.json`, and when the canonical file is missing Core must materialize a fresh normalized `settings.json` during startup/settings bootstrap instead of resurrecting any legacy filename.
   - Broadcast contract (1.2.13): **любой** `session:model:update` event, отправляемый клиенту, обязан нести effective modelId (т.е. с суффиксом `thinking:X` / `reasoning:X` где применимо), а НЕ raw base id от provider SDK. Это распространяется и на `broadcastRuntimeModelUpdate` в `session-provider-event-router.ts` (путь через SDK `model_info` events), и на dispatch-path из `session-request-handler-message-dispatch.ts`. Raw `data.model` от SDK должен обогащаться через `SessionRequestHandlerAppliedTurnConfig.resolveEffectiveModelId(providerId, targetModelId)` до broadcast'а. Без этого UI label в session status panel мерцает между двумя формами (`(thinking high)` vs `(high)`) в зависимости от того, какой update прилетел последним.
   - Client-side fallback contract (1.2.15): клиентский `ModelInfo` builder в `src/client/ui/src/session/model-info-builder.ts` при fallback-пути к `settings` (initial render до первого `session:model:update`) обязан обернуть raw level из `thinkingLevelByModel` / `reasoningByModel` в тот же provider-specific prefix, который эмитит `parseEffectiveModelId` на effective id (`thinking ${level}` для Gemini, `reasoning ${level}` для Codex). Claude ветка сохраняет отдельную convention (`"thinking off"` для disabled, raw effort для enabled). Без этой consistency UI показывает label в двух разных формах за время первого рендера (начальный fallback → Core broadcast).
   - Канон: `doc/SolidWorks-WorkFlow/Contracts/EffectiveModelIdentity_And_Settings_SSOT.md`, `packages/core/src/config/index.ts`, `packages/core/src/config/provider-turn-config-resolver.ts`, `packages/core/src/remote-bridge/handlers/settings-persistence-service.ts`, `src/extension-module/settings/settings-storage.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-applied-turn-config.ts`, `packages/core/src/remote-bridge/handlers/session-provider-event-router.ts`.
15. **Provider-applied model/reasoning proof stays provider-native**: active baseline не поддерживает cross-provider normalizing contract для exact model/thinking/reasoning feedback в `sdk-*` логах; для аудита реально применённого provider state нужно опираться на provider-native runtime artifacts (`Claude` provider-home JSONL, `Codex` raw rollout `turn_context`, `Gemini` raw stream/session traces), а не на локальный outbound intent.
   - Канон: `doc/SolidWorks-WorkFlow/Modules/Claude.md`, `doc/SolidWorks-WorkFlow/Modules/Codex.md`, `doc/SolidWorks-WorkFlow/Modules/Gemini.md`.
16. **Localization source-copy and startup-bootstrap invariant**: product-owned localizable copy must be authored in bundled English source dictionaries, mutable bundles/glossary live only under `~/.codeai-hub/localization/`, persisted startup snapshots live under `~/.codeai-hub/localization/cache/browser-runtime-bootstrap.json`, and browser lookup must consume host-delivered bootstrap/runtime payloads instead of reading mutable localization files directly; fallback strings are last-resort bootstrap-only, and React components are not allowed to become the source of truth for localizable product copy. New keys must go into **approved** source dictionary files only (`ui_labels.json`, `ui_helper_text.json`, `messages_for_the_user.json`, `artifacts_for_the_user.json`); legacy files are shadowed by the registry when an approved file exists (see §4.5 of the contract).
   - Канон: `doc/SolidWorks-WorkFlow/Modules/Localization.md`, `doc/SolidWorks-WorkFlow/Contracts/UserFacing_Text_Localization_Boundary.md`.
17. **Explicit text-ownership invariant**: every new product-authored text surface must be classified up front as `UI Labels`, `UI Helper Text`, `Messages for the User`, `Artifacts for the User`, or `Internal Agent Instructions`. Category guessing and deferred cleanup are not acceptable.
   - Канон: `doc/SolidWorks-WorkFlow/Contracts/UserFacing_Text_Localization_Boundary.md`.
18. **Workflow artifact-language boundary**: `Artifacts for the User` may drive questionnaire/staged-artifact shell text and brief user-facing workflow chat updates, but internal workflow/provider prompt bodies remain `Internal Agent Instructions` and stay English-only.
   - Канон: `doc/SolidWorks-WorkFlow/Modules/Localization.md`, `doc/SolidWorks-WorkFlow/Contracts/UserFacing_Text_Localization_Boundary.md`.
19. **New workflow-step rollout must clone a mature reference-step contract end-to-end**: introducing a new stage is not allowed to stop at toolbar visibility, prompt wiring, or artifact creation. A supported new step must ship with synchronized stage identity, artifact paths, PM parity, localization ownership, continuity routing, handoff paths, cold-start persistence, direct regression coverage, and packaged-release validation. Partial stage shells are not acceptable.
   - Канон: `doc/SolidWorks-WorkFlow/System/Workflow_NewStep_Rollout_Guardrails.md`, `doc/SolidWorks-WorkFlow/Contracts/Workflow_CLI.md`, `doc/SolidWorks-WorkFlow/Contracts/Dialogs_And_Continuity_Routing.md`.
20. **Development Tree read model and sidebar-only navigation**: the top stage toolbar is removed (v1.1.924). Sidebar Workflow Tree is the sole navigation surface, split into two labeled sections: **Documentation Tree** (trunk stages as leaf nodes) and **Development Tree** (branch nodes). Trunk stages use three-color indicators: gray (idle — nothing exists), orange (in_progress — session started, no final artifact), green (artifact available). On workspace open, sidebar auto-selects the last non-idle stage (diagram_modules → virtual_simulation → description). Zoom badge relocated from diagram canvas to status bar. After Diagram Modules, the workflow-state API exposes a `developmentTree` snapshot with Product Part / Cluster / Module tree structure derived from generated product-part artifacts. The sidebar projects these as collapsible branch nodes with skeleton/materialized/draft status. Branch-node selection dispatches `pm:branch:selected` and updates the main area panel header with a placeholder surface.
   - Канон: `doc/SolidWorks-WorkFlow/Plans/DevelopmentTree_BranchWorkflow_Architecture.md`, `doc/SolidWorks-WorkFlow/Plans/DevelopmentTree_Sidebar_Visualization_Architecture.md`.
21. **Translation execution-mode invariant**: live/user-facing translation requests may use shared safe chunk planning with engine-specific conservative budgets, but interface/bootstrap localization is allowed to opt out of chunking and use strict whole-request materialization. Interface bundle sync now uses one structured marker-preserving batch request per bundle plus warm temp Codex-home bootstrap reuse. In both modes, fallback handling must be explicit: live overlays may degrade per message, while startup localization sync may not report ready until required bundles are fully materialized. If the caller explicitly pins an engine, runtime is not allowed to silently substitute another engine; missing explicit engines must fail closed. Core-only engines such as `anthropic-claude-haiku-4-5` must execute through their provider-owned runtime path, not through extension-side local materialization fallbacks.
   - Канон: `doc/SolidWorks-WorkFlow/Modules/Shared_RuntimeTranslation_Module.md`, `doc/SolidWorks-WorkFlow/Modules/Localization.md`.
22. **Localization blocking UI invariant**: busy/save-sync blocking may temporarily hide PM or settings interactive surfaces, but it must not change React hook order or leave the renderer mounted as an empty shell after `busy -> ready`. Blocking placeholders are allowed; hook-graph drift is not. Busy-state copy in the Settings card and the Project Manager blocker must describe incremental sync of the runtime bundles actually affected by the last save, not a blanket rebuild of the entire translated interface — provider-only, response-policy, and continuity-only saves skip the overlay entirely, and engine/category saves rebuild only the planned bundle set. Save-impact scope is UI-owned only: `LocalizationSettingsImpactClassifier` inspects the UI translation engine id (`uiEngineId`), the glossary-enabled flag, and the four UI-owned category languages (`UI Labels`, `UI Helper Text`, `Messages for the User`, `Artifacts for the User`). The reasoning translation engine id (`reasoningEngineId`) and the fifth user-facing `Reasoning` category language are runtime-only: changing them never enters the strict sync path, never blocks PM/new session sends, and never rebuilds browser bootstrap bundles.
   - Канон: `src/client/project-manager/components/layout/main-area-panel-content.tsx`, `src/client/ui/src/components/settings-view.tsx`, `src/extension-module/settings/localization-settings-impact-classifier.ts`, `src/extension-module/settings/general-settings.ts`, `doc/SolidWorks-WorkFlow/Modules/Localization.md`.
23. **Thinking/Reasoning emission-time visibility invariant**: visible provider `Thinking / Reasoning` bubbles must carry an immutable `visibilityAtEmission` decision on the persisted `SessionMessage`. The decision is resolved at append time from the active settings snapshot (Claude/Gemini `thinkingDisplaySyncEnabled`, Codex `reasoningSummaryEnabled`), persisted through both the Core `SessionMessage` record and the UI wire contract, and consumed by the shared Session transcript filters through `shouldHideThinkingMessage`. Current settings toggles no longer reinterpret visibility for messages that were emitted earlier: hidden-at-emission thinking stays hidden after `Thinking in dialog` / `Reasoning in dialog` is re-enabled, and visible-at-emission thinking stays visible after the toggle is disabled. Messages persisted before the stamping was introduced still fall back to the current settings flag. Translation dispatch also honors emission-time visibility so hidden thinking never enters the Core overlay translation queue.
    - Канон: `packages/core/src/session-manager/index.ts`, `packages/core/src/session-translation/session-translation-facade.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-event-messages.ts`, `src/types/session.ts`, `src/client/ui/src/core-bridge/normalizers.ts`, `src/client/ui/src/session/helpers.ts`, `src/client/ui/src/session/session-view-helpers.tsx`, `src/client/ui/src/session/virtual-conversation-message-utils.ts`.
24. **Provider Stop is shutdown-safe AND actually aborts the active turn**: a `Stop` from `Project Manager` interrupts the active provider turn and is allowed to surface as `terminal_reason = "aborted_streaming"` on the SDK level. After `session.turnQueue.shutdownRequested` is set, late provider errors (processor / dispatch / processing) MUST NOT be emitted into the session error channel; the channel is being torn down and an emission would surface as Node `ERR_UNHANDLED_ERROR` and crash core. Pre-shutdown provider errors continue to flow through the standard provider error envelope and reach Core via the per-adapter error bridge so `Stop -> Continue` can rebind to the same workflow continuity chain on the next user message.
    - Critically, `adapter.closeSession` MUST actually interrupt the underlying provider work, not just resolve the outer message generator. For the active Codex line this means `packages/Codex_AppServer_Module/src/app-server/codex-app-server-facade.ts` must send `turn/interrupt(threadId, turnId)` against the long-lived `codex app-server` process before tearing the logical session down; `packages/Codex_AppServer_Module/src/app-server/process/codex-app-server-process.ts` keeps that child alive until the last logical Codex session closes and then terminates it. Without the interrupt, Stop would again degrade into waiting for provider-natural completion. Legacy `killActiveCodexProcess(threadId)` via `codex exec` subprocess is no longer the canonical runtime path for the active release line.
    - PM side: the `Stop` button in `InputPanel` must debounce while a stop is in flight — tracked via `stopInFlight` state that resets when `agentBusy` transitions to false. Without debounce, repeated Stop clicks stack parallel `handleStop` invocations in Core. Core's `handleStop` also early-returns on `hasStopInvalidatedBinding(sessionId)` so any bypass of the PM debounce does not re-invalidate an already-pending binding.
    - Gemini branch (1.2.7+): `Stop` for `geminiCli` is NOT allowed to call `session.client.resetChat()` in `packages/Gemini_Module/src/session/gemini-session-lifecycle.ts`. On the Gemini CLI Core side `resetChat` materializes a fresh empty `GeminiChat` against the same `Config.sessionId` and writes a new empty chat file under `~/.gemini/tmp/<projectSlug>/chats/`, orphaning the prior chat-file history and losing the original workflow system instruction. Abort via `abortController.abort()` is the only permitted cancellation path; the pre-stop chat file must stay intact on disk so `gemini --resume <providerSessionId>` can reload it on the next turn. Core-side post-stop rebind for providers declaring the `requiresPostStopResume` capability (currently only `geminiCli`) MUST persist the pre-stop `providerSessionId` in `SessionProviderBindingService.preStopProviderSessionIdBySession` right before `invalidateProviderBinding` and thread it back into `resolveProviderSessionId` as `requestedProviderSessionId` inside `SessionRequestHandlerStopRebind.performRebind`; `GeminiProviderAdapter.resumeSession` forwards it as `argv.resume`. **Gemini real resume (1.2.8):** `argv.resume` alone is a no-op on the embedded cli-core path because the official `gemini` binary main is the one that runs `SessionSelector.resolveSession(uuid)` → `config.setSessionId(...)` → `client.resumeChat(convertSessionToClientHistory(messages), resumedSessionData)`. `GeminiSessionBootstrapper` in `packages/Gemini_Module/src/session/gemini-session-bootstrapper.ts` MUST perform the same pipeline itself (scan `config.storage.getProjectTempDir()/chats` for `session-*-<uuid-first-8>.json`, pick the file whose full `sessionId` matches and has the most messages, call `setSessionId`, `sessionUtils.convertSessionToClientHistory`, then `await client.resumeChat(history, { conversation, filePath })`); only that call routes `ChatRecordingService.initialize` into the resume branch which reuses the existing chat file. Failure paths (missing sessionUtils export, no matching file, malformed JSON) degrade to a warn-logged fresh session. **Stale-seed auto-recovery (1.2.8):** when Project Manager dialog bootstrap seeds a new Core session with a dead `providerSessionId` + `providerSessionStatus: "ready"`, the subsequent send bypasses `hasStopInvalidatedBinding` and hits the provider directly. Providers with `requiresPostStopResume` must therefore publish a recognizable stale-binding surface — `GeminiSessionStaleBindingError` with `code: "GEMINI_SESSION_STALE_BINDING"` and a `providerSessionId` field — so `SessionRequestHandlerMessageDispatch.dispatchUserMessage` can catch it once per turn, invalidate the binding (capturing the pre-stop id), re-run `ensureSessionReadyForSend` to trigger the resume path, and retry the send. A second failure flows through as an ordinary provider error. **Installer layout (1.2.8):** `GEMINI_INSTALLER_PATHS` points to the full module subpath `<providers-home>/gemini/cli/lib/node_modules/@google/gemini-cli/` so `GeminiInstaller.computePrefix` strips `/node_modules/...` + `/lib` and resolves `npmPrefix` to `<providers-home>/gemini/cli`, nesting the CLI's `bin/` and `lib/` inside the Gemini provider umbrella instead of scattering them at the providers-home root. Providers with `requiresPostStopResume = false` (Claude, Codex) continue through the legacy `null` requestedProviderSessionId path because their post-stop continuity is owned provider-natively (Claude SDK resume against provider-home JSONL; Codex keeps provider-owned thread continuity inside the app-server transport and does not require the Gemini-style pre-stop requestedProviderSessionId path).
    - Канон: `packages/Claude_Module/src/messaging/message-processor.ts`, `packages/Claude_Module/src/provider/claude-provider-adapter.ts`, `packages/Codex_AppServer_Module/src/provider/codex-provider-adapter.ts`, `packages/Codex_AppServer_Module/src/app-server/codex-app-server-facade.ts`, `packages/Codex_AppServer_Module/src/app-server/codex-app-server-event-router.ts`, `packages/Codex_AppServer_Module/src/app-server/process/codex-app-server-process.ts`, `packages/Gemini_Module/src/session/gemini-session-lifecycle.ts`, `packages/Gemini_Module/src/session/gemini-session-manager.ts`, `packages/Gemini_Module/src/session/gemini-session-bootstrapper.ts`, `packages/Gemini_Module/src/provider/gemini-provider-adapter.ts`, `packages/Gemini_Module/src/provider/gemini-session-stale-binding-error.ts`, `packages/Gemini_Module/src/session/gemini-session-settings-resolver.ts`, `packages/Gemini_Module/src/runtime/cli-bridge-module-loader.ts`, `packages/Gemini_Module/src/runtime/cli-types.ts`, `packages/core/src/provider-registry/provider-descriptor-factory.ts`, `packages/core/src/provider-registry/provider-installer-paths.ts`, `packages/core/src/provider-registry/provider-module-loader.types.ts`, `packages/core/src/remote-bridge/handlers/session-provider-binding-service.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-stop-action.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-stop-rebind.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-message-dispatch.ts`, `src/client/ui/src/session/input-panel.tsx`, `src/client/ui/src/session/input-play-stop-button.tsx`, `doc/SolidWorks-WorkFlow/Modules/Claude.md`, `doc/SolidWorks-WorkFlow/Modules/Codex.md`, `doc/SolidWorks-WorkFlow/Modules/Gemini.md`.
25. **Provider live content is incremental and dedup-safe**: providers must surface both visible reasoning AND visible assistant text incrementally, rather than as one final monolithic block, whenever the upstream stream supports delta events. For Claude this is owned by `ClaudeContentStreamHandler` over `ClaudeThinkingLiveBuffer` + `ClaudeTextLiveBuffer`: the handler materializes sentence/paragraph segments from `thinking_delta` / `text_delta` fragments, emits them as append-only thinking dialog bubbles (thinking path) or live assistant text bubbles (text path), and reconciles the final assembled block against the materialized prefix so the same content never appears twice in the dialog. For text, live emission eliminates the multi-minute silence that otherwise occurs when Claude streams large `input_json_delta` payloads for `Write/Edit` tool_use after a pre-tool text block, but localized Cyrillic-target sessions now apply a narrow hold on source-language pre-tool `text_delta`: if the pending text still looks non-target-language, it must stay off the assistant/live path until `message_delta.stop_reason` classifies the message. `tool_use` preambles resolve through the thinking contract and must not surface as standalone assistant bubbles between thinking chunks; ordinary `end_turn` assistant output still resolves through the assistant path. Core-owned translation overlays must be ready to attach `localizedContent` to multiple thinking/text bubbles per turn, each carrying its own stable `messageId`, while provider-local Claude thinking translation remains allowed for final assembled thinking / tool-use preamble text before emit. Live assistant bubbles are emitted with `tag: "live"` which Core `appendProviderMessage` forwards into `SessionMessage.tag`; the UI layer then collapses consecutive live bubbles into one growing dialog card through `mergeLiveAssistantMessages`, symmetric to how `mergeThinkingMessages` already collapses consecutive thinking bubbles. The per-segment append-only persistence contract stays intact so translation overlays still attach `localizedContent` per segment.
    - Канон: `packages/Claude_Module/src/messaging/claude-thinking-live-buffer.ts`, `packages/Claude_Module/src/messaging/claude-text-live-buffer.ts`, `packages/Claude_Module/src/messaging/claude-content-stream-handler.ts`, `packages/Claude_Module/src/messaging/claude-stream-event-router.ts`, `packages/Claude_Module/src/messaging/claude-thinking-dialog-emitter.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-event-messages.ts`, `src/client/ui/src/session/dialog-panel-message-utils.ts`, `src/client/ui/src/session/dialog-panel.tsx`, `doc/SolidWorks-WorkFlow/Modules/Claude.md`, `doc/SolidWorks-WorkFlow/Modules/Shared_RuntimeTranslation_Module.md`.
26. **Claude model aliases stay unversioned in UI**: `CLAUDE_MODEL_ALIASES[].displayName` exposes only `Sonnet` / `Opus` / `Haiku`; the Claude Agent SDK resolves the alias to the latest concrete model at query time (`opus → claude-opus-4-7` as of this release). UI and persisted snapshots SHOULD NOT encode a fixed numeric version — the alias IS the contract. `ClaudeThinkingEffort` union accepts 5 levels — `low | medium | high | xhigh | max` — with `xhigh` being an Opus-only "Deeper than high" tier that falls back to `high` on other models. `thinking.display = "summarized"` is forwarded to the SDK whenever thinking is enabled so Opus 4.7 actually emits plain-text `thinking_delta`.
    - Канон: `src/types/claude-model-registry.ts`, `packages/core/src/config/provider-defaults-resolver.ts`, `packages/Claude_Module/src/sdk/claude-sdk-manager.ts`, `src/client/ui/src/components/settings/thinking/thinking-effort-selector.tsx`.
27. **Thinking/reasoning effort whitelists must stay in lockstep across Extension and Core**: `settings.json` is parsed and re-normalized by at least TWO independent layers — the extension-side `parseSettingsSnapshot` (on save from VSCode Settings UI) AND the Core-side `SettingsRequestHandler.handleLoad` (on PM / websocket `settings:load`). Any provider effort/thinking value that the Extension accepts but Core does not is silently rewritten back to the Core default and persisted to disk — effectively reverting the user's save on the next PM boot. When a new Claude `ClaudeThinkingEffort`, Codex reasoning level, or Gemini thinking level is introduced, **all four** of these locations must be updated in the same change:
    - UI-side registry: `src/types/claude-model-registry.ts` (`CLAUDE_THINKING_EFFORT_SET`), `src/types/codex-model-registry.ts` (if applicable), `src/types/gemini-model-registry.ts` (if applicable).
    - Extension-side normalizer: `src/extension-module/settings/claude-settings.ts` / `codex-settings.ts` / `gemini-settings.ts` (and `packages/core/src/config/provider-defaults-resolver.ts` shared with Core).
    - Core remote-bridge normalizer: `packages/core/src/remote-bridge/handlers/settings-request-handler-claude-thinking.ts` (and any future `-codex-reasoning.ts` / `-gemini-thinking.ts` sibling handler). These Core handlers currently carry their own hardcoded whitelist + legacy-token-anchor table and must mirror the canonical set. Core's `handleLoad` writes the normalized snapshot back to disk when `changed=true`, so a missing value here causes a silent save reversion on PM startup (regression introduced in 1.1.998 `xhigh`, fixed in 1.2.2).
    - Release checklist item: when adding an effort level, grep the repo for the existing whitelist (e.g. `"high", "max"`) and update every hit in the same commit.
    - Канон: `packages/core/src/remote-bridge/handlers/settings-request-handler-claude-thinking.ts`, `packages/core/src/remote-bridge/handlers/settings-request-handler.ts`, `src/extension-module/settings/claude-settings.ts`, `src/extension-module/settings/codex-settings.ts`, `src/extension-module/settings/gemini-settings.ts`, `packages/core/src/config/provider-defaults-resolver.ts`, `src/types/claude-model-registry.ts`, `doc/SolidWorks-WorkFlow/Modules/Claude.md`, `doc/SolidWorks-WorkFlow/Modules/Codex.md`, `doc/SolidWorks-WorkFlow/Modules/Gemini.md`.
28. **Post-stop rebind session adoption**: после `Stop` Core инвалидирует provider binding (`providerSessionId → null`, `status → pending`) и при следующем user message создаёт новую logical session с тем же provider-native session id. PM dialog controller (`useProjectManagerDialogSessionController`) обязан этот swap принять: `onSessionBinding` должен обновлять binding не только в snapshot, но и в `SessionRecord`, плюс запоминать pre-stop `providerSessionId` на момент его reset к `null`. `onSessionCreated` обязан adopt-ить новую session, если её `providerSessionId` совпадает с запомненным. Иначе UI продолжает смотреть мёртвую старую session (`idle`), не зная, что Core шлёт running на новую, — input panel не блокируется, `Agents is working, please wait...` не активен. Регрессия введена в базовом Stop/Continue flow и была невидима до 1.2.3 / 1.2.4 диагностики; fixed в 1.2.5. Codex `adapter.closeSession` отдельно имеет baseline-проблему (не abort-ит активный turn, Stop-клики аккумулируются до естественного `turn_completed`) — scope для следующего release.
    - Канон: `src/client/project-manager/components/sessions/use-project-manager-dialog-session-controller.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-stop-action.ts`, `packages/core/src/remote-bridge/handlers/session-request-handler-stop-rebind.ts`.
29. **Acceptable parallel-scaffolding duplication** (1.2.10): CodeAI Hub монорепо содержит ~200 клонов кода, и это **архитектурно оправданно**. Дубликация НЕ считается долгом и НЕ рефакторится в случаях: (а) параллельный boilerplate между Claude/Codex/Gemini провайдерными модулями (installer, session-logger, provider-adapter, session-registry, auth bridge, npm-runner) — каждый модуль владеет своей копией ради module isolation; попытка extract в shared нарушит модульные границы; (б) симметричные копии через architectural boundary (client ↔ core, extension ↔ client) — идентичные type-guards и type unions намеренно дублируются в обоих слоях как authoritative sources своих layers; (в) похожий код разных доменов (два разных DSL parser'а, два разных emitter'а) — extract сольёт семантику и потеряет clarity. Аудит classify'ит top-N клонов через `PeriodicAudit.md` rubric (LEGIT-PROVIDER / LEGIT-BOUNDARY / LEGIT-SIMILAR-BUT-DIVERGING vs EXTRACT-EASY / EXTRACT-COMPLEX / WITHIN-FILE-BUG) и трогает **только** EXTRACT-* и WITHIN-FILE-BUG. Провал `jscpd` порога (>3%) или подозрение на drift между parallel implementations — основание для targeted extract, но не для массового cleanup. Target `check:dup` — под 3% с запасом; после 1.2.10 — 1.97%.
    - Канон: `doc/SolidWorks-WorkFlow/Checklists/PeriodicAudit.md`, `doc/TODO/Archive/todo-plan-1.2.10-audit-cleanup.md` (precedent cycle).
30. **Provider uncaughtException safety** (1.2.12): Core daemon process владеет глобальным `process.on("uncaughtException", ...)` handler'ом, который **selectively** суппрессит только `AbortError` из embed'нутых provider SDK stack'ов. Необходимость: embed'нутые provider SDK (сейчас — `@google/gemini-cli-core`) держат background Promise chains параллельно нашему turn runner, и их internal aborts (например `GeminiClient.processTurn` → `controller.abort()` на loop detection) **не попадают** в try/catch per-turn iterator. Без handler'а Node default — crash daemon. Handler сравнивает `error.stack` с allowlist `PROVIDER_ABORT_SUPPRESSION_STACKS` в `packages/core/src/index.ts`; matched abort пишется в fatal-log как `core:abortError:suppressed` и swallow'ится. Любой другой uncaughtException передаётся наверх (rethrow), что сохраняет default Node crash для real bugs и существующий `uncaughtExceptionMonitor` продолжает их логировать. Добавление нового provider SDK в allowlist — explicit decision, не silent catch.
    - Канон: `packages/core/src/index.ts`, `doc/SolidWorks-WorkFlow/Modules/Gemini.md`.
31. **Replay-first usage telemetry and PM polling budget** (1.2.18): `usage_limits` и `token_usage` — это lifecycle-owned session signals, а не UI-owned polling state. Session UI surfaces в PM (`SessionIdBar`, dialog/runtime session views) могут только отображать cached/replayed snapshots и не имеют права инициировать provider refresh на mount/rebind/rerender. На `binding_ready`, `session_opened`, `provider_session_rebound` и `reconnect` Core обязан сначала replay'ить cached `usage_limits`, а automatic bootstrap refresh разрешён только один раз на конкретный ready-binding lifecycle и только если cache отсутствует. `dialog_opened` — explicit user-intent trigger: Core сначала replay'ит cached `usage_limits`, а затем делает cheap provider refresh даже при наличии cache, чтобы reopened dialog показывал last-known limits immediately и догонял freshness до нового turn. Late `usage_limits` payload остаётся provider-scoped truth, а не session-scoped truth: если исходный session snapshot уже заменён другим runtime id того же provider family, PM всё равно обязан применить свежий payload к текущему visible snapshot и не ждать повторного reopen. `turn_completed` остаётся единственной unconditional automatic boundary для свежего usage telemetry. Idle open session без нового lifecycle trigger не должна reread'ить provider limits. PM background observers (`workflow-state-store`, `workflow-events-client`, artifact availability probes, diagram progress probes) обязаны быть visibility-aware: `foreground` использует normal cadence, `background` замедляется до `30s`, `hidden` паркует polling до возврата окна, а возврат в `foreground` делает immediate catch-up вместо параллельных fast loops.
    - Канон: `doc/SolidWorks-WorkFlow/Clusters/CoreOrchestrator.md`, `doc/SolidWorks-WorkFlow/Clusters/Project_Manager.md`, `doc/SolidWorks-WorkFlow/Contracts/SessionUI_Behavior.md`, `doc/SolidWorks-WorkFlow/Contracts/WorkspaceRuntime.md`.
32. **CEF macOS launcher: plain NSApplication + CanClose short-circuit + reportException: swizzle safety net** (1.2.49 baseline, 1.2.52 primary fix): попытка ввести custom `NSApplication <CefAppProtocol>` shell в 1.2.46 сломала clipboard shortcuts; 1.2.48 narrow fix не помог; весь refactor отозван в 1.2.49. Canonical state: `packages/cef-launcher/src/platform/mac/app_main_mac.mm` использует обычный `[NSApplication sharedApplication]` без `CefAppProtocol` subclass и без `NSApplicationDelegate`. User retest установил crash trigger: детерминирован только при клике по красной NSWindow close кнопке (`LauncherWindowDelegate::CanClose` → `browser->GetHost()->TryCloseBrowser()` → Chromium 141 async browser-teardown callback), НЕ воспроизводится на `Cmd+Q` / Dock Quit (которые идут через `-[NSApplication terminate:]` → `-[NSApplication stop:]` и обходят buggy Chromium teardown path). Root cause — Chromium 141 (`141.0.10+chromium-141.0.7390.123` shipped inside CEF) отправляет AppKit-private selector, который больше не существует на macOS 26.3.1; proper root-cause fix требует CEF/Chromium upgrade и остаётся deferred. Две mitigation-попытки через exception pipeline не сработали: 1.2.50 `NSSetUncaughtExceptionHandler()` (AppKit перезаписывает handler на `finishLaunching`; `+[NSApplication _crashOnException:]` обходит стандартную uncaught chain на macOS 26); 1.2.51 `+load`-swizzle на `-[NSApplication reportException:]` (user retest показал что crash всё равно появляется — exception на macOS 26 апparently достигает `_crashOnException:` не только через `reportException:`). Primary fix 1.2.52 — **короткозамыкание buggy Chromium callback на source-level**: `LauncherWindowDelegate::CanClose` в `packages/cef-launcher/src/launcher_app.cc` на macOS (`#if defined(__APPLE__)`) вместо `TryCloseBrowser()` вызывает cross-platform helper `codeai::launcher::RequestNativeApplicationTermination()` (declared в `launcher_handler.h`, implemented в `launcher_handler_mac.mm` через `[NSApp terminate:nil]`) и возвращает `false`. Красная close кнопка теперь идёт по тому же Cmd+Q path — `-[NSApplication terminate:]` → `-[NSApplication stop:]` — не запуская buggy teardown callback. Exception физически не кидается. Windows/Linux branch оставлен без изменений. Swizzle 1.2.51 remains как belts-and-suspenders safety net на случай если в CEF views framework остался ещё один path, триггерящий тот же exception signature. Mitigation не меняет NSApplication class, Info.plist, `LauncherHandler::DoClose` / `OnBeforeClose` / `CloseAllBrowsers`. Acceptance matrix для любого будущего CEF bootstrap изменения: Cmd+V/C/X/A + SuperWhisper + Cmd+Q + Dock Quit + red close button без crash dialog + dock reopen.
    - Канон: `doc/SolidWorks-WorkFlow/Modules/Launcher_CEF.md`, `doc/BugRegistry.md` (`BUG-2026-04-22-01`, `BUG-2026-04-22-04`), `packages/cef-launcher/src/launcher_app.cc` (`LauncherWindowDelegate::CanClose`), `packages/cef-launcher/src/launcher_handler.h` (`codeai::launcher::RequestNativeApplicationTermination`), `packages/cef-launcher/src/platform/mac/launcher_handler_mac.mm`, `packages/cef-launcher/src/platform/mac/app_main_mac.mm` (swizzle safety net), `doc/SolidWorks-WorkFlow/Plans/Archive/CEF_MacOS_BootstrapHardening_Architecture.md`, `doc/SolidWorks-WorkFlow/Plans/Archive/CEF_MacOS_Input_And_Quit_Regression_Architecture.md`, `doc/SolidWorks-WorkFlow/Plans/Archive/CEF_MacOS_Bootstrap_Rollback_Architecture.md`, `doc/SolidWorks-WorkFlow/Plans/Archive/CEF_MacOS_Shutdown_Crash_Mitigation_Architecture.md` (1.2.50 failed), `doc/SolidWorks-WorkFlow/Plans/Archive/CEF_MacOS_ReportException_Swizzle_Architecture.md` (1.2.51 failed alone, retained as safety net), `doc/SolidWorks-WorkFlow/Plans/Archive/CEF_MacOS_CanClose_ShortCircuit_Architecture.md` (1.2.52 primary fix).
33. **Settings ownership invariant** (1.2.53/1.2.54, native request capture extended through 1.2.98): Core является единственным backend owner для settings lifecycle (`settings:load`, `settings:save`, `settings:reset`, `settings:update-provider`, `settings:versions`, `settings:open-user-glossary-file`, `settings:native-request-capture`) и для связанных broadcasts (`settings:loaded`, `settings:saved`, `settings:save-error`, `settings:localization-sync-status`, `settings:user-glossary-file`, `settings:native-request-capture:result`). Project Manager является единственным живым Settings UI: footer action `Open Settings` переводит правую панель PM в in-shell settings mode, shared `SettingsView` работает в `mode="project-manager"`, а PM-specific state/transport hooks переводят UI intents прямо в Core remote bridge. Provider auto-update checkbox semantics также принадлежат этому Core-owned settings path: на старте Core читает `~/.codeai-hub/settings/settings.json`, применяет `providers.*.autoUpdate.enabled` через `SettingsProviderAutoUpdateService`, выполняет enabled CLI/SDK update targets и только затем инициализирует provider registry. Provider native request capture является one-shot diagnostic surface в General Settings: UI выбирает provider/model/scenario; `Description`, `Virtual Simulation` и `Diagram Modules` resolve-ятся как реальные `workflow-agent` model invocation profiles и используют workflow first-turn prompt через штатный `buildWorkflowPromptPack(...)`, а `Translation` resolve-ится как реальный `translation` profile с фиксированным малым translation sample. `diagnostic` не является отдельным `purpose` или profile. Core добавляет applied turn config и запускает provider-native capture-and-abort; если provider descriptor известен, но adapter ещё не инициализирован, результат обязан быть `provider_not_ready`, а `provider_not_supported` остаётся только для реально неподдержанного provider/capture method. Detached settings window `?mode=detached-settings` был промежуточным состоянием релиза `1.2.53`, но удалён в `1.2.54` и не является текущим продуктовым контрактом. VS Code extension settings surface остаётся только compat notice и не имеет права владеть save/reset/provider-update flow, runtime restart controls или bootstrap authority.
    - Канон: `doc/SolidWorks-WorkFlow/Clusters/Project_Manager.md`, `doc/SolidWorks-WorkFlow/Modules/UI_Bundles.md`, `packages/core/src/remote-bridge/handlers/settings-request-handler.ts`, `src/client/project-manager/components/settings/use-project-manager-settings.ts`, `src/client/project-manager/components/settings/use-project-manager-settings-state.ts`, `src/client/ui/src/app-host/settings-only-host.tsx`.
34. **CI quality-gate platform-binary invariant** (1.2.58): репозиторный CI (`.github/workflows/ci.yml`, `ubuntu-latest`) использует `npm ci`, который строго следует `package-lock.json`. Наш lockfile генерируется на macOS Apple Silicon; Biome (`@biomejs/biome`) и другие toolchain'ы с native binaries доставляются через platform-specific optional packages (`@biomejs/cli-<os>-<arch>`). Если в lockfile нет записи для CI-платформы (Linux x64), `npm ci` не поставит её, и `biome` shim мгновенно падает `require.resolve` → `Process completed with exit code 1` за 0 секунд, что выглядит как неопределённый "Lint step failed" без visible output. **Contract**: все 7 non-host Biome CLI binaries (`@biomejs/cli-darwin-x64`, `@biomejs/cli-linux-arm64`, `@biomejs/cli-linux-arm64-musl`, `@biomejs/cli-linux-x64`, `@biomejs/cli-linux-x64-musl`, `@biomejs/cli-win32-arm64`, `@biomejs/cli-win32-x64`) обязаны быть объявлены в root `package.json` `optionalDependencies` с exact pinned version (`2.4.7` в 1.2.58), чтобы `npm install`/`npm ci` записал их в `packages` секцию lockfile с os/cpu guards. На каждой платформе npm ставит только свой binary, но lockfile содержит полный набор, что делает CI воспроизводимым на любом runner'е. При bump'е версии Biome **все 8 platform entries** должны обновляться синхронно. Аналогичный инвариант распространяется на любые будущие toolchain'ы с native binaries (swc, esbuild native path, sharp, etc.), если они попадают в lint/compile horn pipeline. Отдельно: CI Knip step (`npm run check:knip`) помечен `continue-on-error: true` — он завершается non-zero на Ubuntu runner по непрозрачной причине (local reproduction даёт exit 0 даже с `CI=true GITHUB_ACTIONS=true`), не заблокирован pre-commit hook (`./.husky/pre-commit` → `npm run check:knip` остаётся strict локально), поэтому advisory-статус не создаёт слепой зоны. Пересматривается при bump'е Knip до major version.
    - Канон: `.github/workflows/ci.yml`, `package.json` `optionalDependencies` block, `package-lock.json` (packages section with os/cpu per `@biomejs/cli-*` entry).
35. **Model invocation profile boundary** (1.2.98): Core owns provider-neutral `ModelInvocationProfile` resolution from provider, model, `purpose`, optional workflow tree, and optional step id. Allowed purposes are only `workflow-agent` and `translation`; diagnostics are a capture mode over one of those real profiles. The profile is split into process/session/turn layers: startup flags, system tools, sandbox and approval policy are code-owned and not user-editable, while user-visible `.md` templates may contribute text-only instruction fragments. Workflow step instructions may be resolved per tree/step, including future Development Tree steps, and translation has its own profile. In-turn model switching may offer only models compatible with the active process/session profile; incompatible models require a new logical step/session instead of silently mutating flags or system tools mid-session.
    - Invocation instruction templates live under `~/.codeai-hub/templates/invocation/<provider>/...` and `~/.codeai-hub/templates/workflow_steps/<tree>/<step>.system.md`. `ModelInvocationInstructionLoader` loads only `source: "user-template"` fragments from these `.md` files; `source: "code"` fragments remain code-owned references and cannot be overridden by user templates.
    - Template sync safety: bundled text templates are tracked by normalized content hash in `~/.codeai-hub/templates/.template-sync-state.json`; clean bundled files may update in place, but user-modified files are preserved and the new bundled candidate is written under `~/.codeai-hub/templates/.incoming/<version>/...` for explicit resolution. Resolution actions are Core-owned and limited to text files: `preserve-current`, `replace-with-incoming`, or `backup-and-replace` under `~/.codeai-hub/templates/.backups/<timestamp>/...`; flags, system tools, sandbox, and approval policy are never exposed through template update decisions. Project Manager transport exposes this through `settings:template-updates` and `settings:template-update:resolve` bridge commands, and the General Settings tab renders the pending update list with both all-files and file-by-file decisions.
    - Канон: `doc/SolidWorks-WorkFlow/Contracts/EffectiveModelIdentity_And_Settings_SSOT.md`, `packages/core/src/model-invocation/model-invocation-profile-resolver.ts`, `doc/SolidWorks-WorkFlow/Plans/ModelInvocationProfiles_And_TemplateSync_Architecture.md`.

## 4) Где искать правду в коде (high-signal)

- Extension entry: `src/extension.ts`
- Core: `packages/core/`
- Provider registry façade cluster: `packages/core/src/provider-registry/`
  - `index.ts` = façade
  - `provider-installer-paths.ts`, `provider-installed-path-resolver.ts`, `provider-module-loader.ts`, `provider-descriptor-factory.ts`, `provider-usage-limits-bridge-factory.ts`, `provider-recovery-{scheduler,coordinator}.ts` = runtime internals
  - `provider-descriptor-factory.ts` now owns provider model-sync capability registration (`acceptsAppliedTurnConfig`, `appliedConfigIdentityKey`, `runtimeModelSelectionKey`, `syncsLabelFromAppliedConfig`), so adding a new provider does not require hidden hardcoded assumptions in remote-bridge/UI glue code about where effective identity lives and which field is still used as the actual runtime model selector
- Core remote bridge cluster: `packages/core/src/remote-bridge/`
  - `index.ts` = thin façade / top-level runtime bridge entrypoint
  - `remote-bridge-bootstrap.ts` = runtime service/bootstrap wiring
  - `remote-bridge-server-lifecycle.ts` = HTTP/WebSocket lifecycle ownership
  - `remote-bridge-message-router.ts` = websocket command orchestration façade; `session:stop` transport branch is registered here as the session-scoped counterpart to global runtime shutdown and intentionally stays separate from `/api/v1/shutdown`
  - `remote-bridge-session-create-router.ts` = isolated `session:create` workflow-binding helper, so create-path orchestration does not keep inflating the top-level websocket command router
  - `remote-bridge-dialog-command-router.ts`, `remote-bridge-workspace-command-router.ts` = scoped websocket command clusters
  - `handlers/settings-request-handler.ts`, `handlers/settings-persistence-service.ts`, `handlers/settings-provider-version-service.ts`, `handlers/settings-provider-auto-update-service.ts`, `handlers/settings-persistence-snapshot.ts` = Core-owned settings transport/persistence cluster; отвечают за load/save/reset/provider update/versions/glossary intents, startup provider auto-update policy, blocking localization sync и downstream broadcasts для PM/web clients
  - `handlers/session-request-handler.ts` = still-oversized orchestration root for dialog send/resume/switch paths; constructor/runtime service-graph wiring now lives in `handlers/session-request-handler-runtime{,-core,-types}.ts`, constructor-local runtime callback assembly now lives in `handlers/session-request-handler-runtime-callbacks.ts`, user-facing switch/message/delete orchestration plus rollover-send guards/logging now live in `handlers/session-request-handler-session-actions.ts`, stop-triggered fresh-provider rebind before resend now lives in `handlers/session-request-handler-stop-rebind.ts`, session-scoped stop invalidation now lives in `handlers/session-request-handler-stop-action.ts`, resume lifecycle state ownership lives in `handlers/session-request-handler-resume-lifecycle.ts`, shell/bootstrap creation path lives in `handlers/session-request-handler-session-bootstrap.ts`, workflow session creation/provider lookup glue now lives in `handlers/session-request-handler-workflow-session.ts`, create/resume/dialog-send resolution lives in `handlers/session-request-handler-session-resolution.ts`, continuity-root resolution and legacy description-root promotion now live in `handlers/session-request-handler-continuity-root.ts`, post-turn continuity arbitration now lives in `handlers/session-request-handler-turn-arbitration.ts`, completion arbitration now lives in `handlers/session-request-handler-turn-completion.ts`, live threshold reload now lives in `handlers/session-request-handler-turn-threshold-resolver.ts`, outbound/internal send orchestration is isolated in `handlers/session-request-handler-message-dispatch.ts`, adapter-send logging/dispatch now lives in `handlers/session-request-handler-provider-send.ts`, Core-owned next-turn applied effective model identity attachment now lives in `handlers/session-request-handler-applied-turn-config.ts`, provider-event message append and incoming payload extraction now live in `handlers/session-request-handler-event-messages.ts`, retry budget + pending-intent TTL state now lives in `handlers/session-request-handler-retry-state.ts`, dialog segment boundary/meta append and latest-segment summary dedupe live in `handlers/session-request-handler-dialog-segment-meta.ts`, and flow-node rollover/report state now lives in `handlers/session-request-handler-flow-node-{rollover,report-state}.ts`
  - `handlers/session-provider-binding-service.ts` + `session-manager/index.ts` now own the stop-invalidation seam for logical sessions: a stop path can drop the live provider binding, broadcast a `pending` binding state, and keep the logical session alive for later rebind instead of deleting it; the same binding service now also tracks one-shot usage telemetry bootstrap per ready provider lifecycle so reopen/rebind can prefer replay over repeated provider refresh
  - `handlers/session-request-handler.ts` + `handlers/websocket-manager.ts` now define replay-first usage telemetry delivery: cached `usage_limits` replay is broadcast before provider refresh, idle sessions skip refresh without a lifecycle trigger, and websocket attach/scope-switch replays the latest `token_usage` / `usage_limits` snapshots (including legacy `usage_limits` payload normalization to `stream_event`)
  - `handlers/localization-bootstrap-http-handler.ts` = authoritative localization bootstrap delivery for Project Manager cold-start hydration before websocket settings refresh; for core-only engines such as `anthropic-claude-haiku-4-5` it rebuilds a strict snapshot from current normalized settings instead of serving persisted cache only
  - `settings:native-request-capture` is a diagnostic settings command owned by `remote-bridge-message-router.ts` plus `packages/core/src/provider-network-capture/native-request-capture-facade.ts`; it starts a local capture-and-abort proxy, carries selected provider/model plus optional workflow scenario metadata (`Description`, `Virtual Simulation`, `Diagram Modules`) and PM-built first-turn prompt, asks the selected provider adapter to run that prompt through the native provider client, and returns `settings:native-request-capture:result` with `.md` / `.jsonl` artifact paths. JSONL is the full-fidelity payload source; Markdown is a readable view that summarizes raw request/ignored/diagnostic payloads and prints large system/tools/messages only in the extracted sections to avoid duplicate prompt copies.
  - `handlers/session-request-handler-applied-turn-config.ts`, `handlers/session-request-handler-message-dispatch.ts`, and `remote-bridge/types.ts` now form one provider-neutral outbound bridge contract: Core attaches one next-turn identity descriptor derived from `settings.json`; presentation/runtime-localization payload such as `thinkingDisplaySyncEnabled`, `reasoningEngineId` / `reasoningLanguage` (the dedicated reasoning translation fields after the UI/Reasoning split), and their deprecated legacy aliases `translationEngineId` / `messagesForTheUserLanguage` may travel on the same applied-config envelope, but they never participate in `modelId` computation; both legacy aliases carry the same resolved value as the reasoning fields until provider adapters finish migrating; Codex reasoning-summary visibility is resolved by the Codex runtime from shared settings, with non-Spark turns sending `summary = detailed|none` and Spark relying on provider-home `model_reasoning_summary`; outbound `session:model:update` normalizes effective identity so public `modelId` is always the runtime identity, while `baseModelId` remains optional auxiliary metadata
  - `types.ts` = thin aggregation surface for remote-bridge contracts, including the applied provider turn-config payload threaded through outbound send/switch paths
  - `types.ts` = thin aggregation surface for remote-bridge contracts
  - `session-stream-contracts.ts`, `workspace-stream-contracts.ts` = stream-scoped contract modules; `session-stream-contracts.ts` now reserves `session:stop` for logical-session stop/cancel traffic instead of reusing global runtime-control paths, and `session:model:update` is the runtime identity event that must carry the same effective `modelId` the provider will actually use on the next turn; `baseModelId` may be present only as optional auxiliary metadata
- Workspace runtime cluster: `packages/core/src/workspace-runtime/`
  - `workspace-runtime-facade.ts` = thin façade / module entrypoint
  - `workspace-runtime-session-sync.ts` = workspace selection, snapshot push, session-store sync
  - `workspace-runtime-lock-sync.ts` = lock/runtime projection and task-timer persistence
- Config cluster: `packages/core/src/config/`
  - `index.ts` = thin config façade / environment assembly entrypoint
  - `provider-settings-snapshot.ts` = persisted provider settings readers; Claude/Gemini thinking-display flags are backfilled on settings load so the on-disk snapshot stays aligned with the UI toggle, while Codex reasoning-summary visibility is now resolved by the Codex runtime from the same persisted settings and mirrored into provider-home config materialization
  - `provider-defaults-resolver.ts` = provider default model/reasoning normalization
  - `provider-turn-config-resolver.ts` = Core-owned registry/resolver for next-turn Claude/Codex/Gemini effective model identity from persisted `~/.codeai-hub/settings/settings.json`; it derives `baseModelId`, effective identity descriptor, provider-specific reasoning/thinking payload, and presentation-only thinking display sync gate from one settings snapshot, while remote-bridge queries one provider-neutral `byProviderId` registry instead of growing new `if (providerId === ...)` branches; the Session UI now uses that gate only to decide whether to render thinking bubbles, not to stop JSONL/history persistence
- Model invocation profile cluster: `packages/core/src/model-invocation/`
  - `model-invocation-profile-resolver.ts` = provider-neutral resolver for `workflow-agent` and `translation` profiles; keeps `diagnostic` out of the profile vocabulary, exposes compatible model lists for safe in-turn model switching, and separates code-owned process/session flags from user-editable text instruction fragments
- Project Manager UI: `src/client/project-manager/` (sidebar-only navigation since v1.1.924; top stage toolbar removed; sidebar split into Documentation Tree / Development Tree sections)
  - `components/sessions/use-project-manager-dialog-session-controller.ts` + `components/sessions/use-project-manager-dialog-core-events.ts` now suppress idle dialog restore churn: completed dialogs may keep bootstrap-ready snapshots, but repeated `dialog:list` / `dialog:message` / remount paths must not synthesize a new runtime restore on their own
  - `services/workflow-state-store.ts`, `services/workflow-events-client.ts`, `components/layout/use-artifact-availability.ts`, and `components/diagram-editor/diagram-stage-panel-scaffold.tsx` now implement the shared visibility-aware PM polling budget (`foreground` normal cadence, `background` `30s`, `hidden` parked, foreground wake-up = immediate catch-up)
- Shared Session UI: `src/client/ui/src/`
  - `src/client/ui/src/session/session-id-bar.tsx` = display-only usage telemetry surface; automatic `usageLimits` refresh ownership does not belong to the UI mount lifecycle
- Localization package: `packages/localization/`
  - `src/localization-facade.ts` = thin public package façade
  - `src/localization-contract.ts`, `src/localization-facade.ts` = runtime payload contract plus materialized bundle resolution
  - `src/localization-materializer.ts`, `src/source-dictionary-registry.ts`, `src/glossary-*.ts`, `src/localization-*-store.ts` = source dictionaries, glossary protection, bundle persistence, metadata reuse
- Shared translation package: `packages/translation/`
  - `src/translation-facade.ts` = thin facade that now resolves engine-specific chunk policies, assembles per-chunk translation results into one request-level outcome, and fail-closes explicit missing `engineId` requests instead of silently substituting the default engine
  - `src/translation-engine-profile-registry.ts`, `src/translation-chunk-boundary-resolver.ts`, `src/translation-chunk-planner.ts` = internal chunk-budget registry plus safe-boundary planning path used before dispatching long translation requests
  - `src/codex-translation-runtime-home-facade.ts` = temp Codex translation runtime materializer that now reuses warm plugin/bootstrap artifacts from resolved Codex home before each `codex exec`
- Browser localization lookup: `src/client/ui/src/app-host/use-localization.ts`
  - shared browser-side localization runtime consumed by settings host and localized PM surfaces
  - `packages/core/src/remote-bridge/handlers/settings-request-handler.ts` остаётся authoritative source для PM settings/runtime localization payloads; extension-side compat surface может читать persisted bootstrap, но не владеет write/localization-sync path
  - `src/core/webview-module/webview-html-generator.ts` injects `window.__CODEAI_LOCALIZATION_BOOTSTRAP__` into Settings WebView HTML when a persisted startup snapshot exists
  - `src/client/ui/src/index.tsx` and `src/client/project-manager/index.tsx` perform pre-render localization bootstrap hydration, while `/api/v1/localization/bootstrap` feeds the Project Manager cold-start path; for `anthropic-claude-haiku-4-5` this endpoint is the authoritative Core-owned bootstrap source
  - `src/client/ui/src/app-host/settings-only-host.tsx` and `src/client/project-manager/app.tsx` provide one shared `LocalizationProvider` at the app boundary; localized PM leaves consume that provider instead of loading settings locally
  - settings save-path now performs a blocking strict localization sync before emitting `settings:saved`; Project Manager and new session sends stay blocked until required translated bundles are ready
  - PM main-area localization blocking must keep hook execution unconditional across busy/ready transitions; blocked placeholders may replace content, but renderer structure must stay stable
  - bundled English source catalogs are no longer embedded as live browser data; component fallback strings remain only as a last-resort safety path when neither persisted bootstrap snapshot nor refreshed runtime payload is available
- Provider settings UI: `src/client/ui/src/components/settings/` and `src/client/ui/src/components/settings-view.tsx`
  - живой product surface принадлежит Project Manager: `src/client/project-manager/app.tsx`, `components/layout/status-bar.tsx`, `components/layout/main-area.tsx`, `components/layout/main-area-panel-content.tsx`, `components/settings/use-project-manager-settings.ts`, `components/settings/use-project-manager-settings-state.ts`
  - shared `SettingsView` reuse-ится через `mode="project-manager"` внутри правой панели PM; footer action `Open Settings` больше не открывает отдельный popup, а переводит right-panel surface в in-shell settings takeover
  - PM general tab снова использует shared `Core Controls`: user-facing settings surface держит persisted/general/localization/provider flows и один recovery action `Restart Core`, но не экспонирует attach/startup diagnostics или extension-era lifecycle controls
  - General Settings now also exposes `Provider Native Request Capture` with Claude/Codex model selectors, scenario selector (`Description`, `Virtual Simulation`, `Diagram Modules`, `Translation`), and capture buttons; the selected capture model/scenario are diagnostic-only and are not persisted as provider defaults
  - `src/client/ui/src/app-host/settings-only-host.tsx` is compat-only notice inside VS Code webview and does not expose live save/reset/provider-update/restart controls
  - the localization card now exposes engine selection through a catalog-backed selector and language selection through a searchable combobox; visible `English` maps to canonical persisted `source`
  - the current user-visible engine set is `Google GTX Free`, `OpenAI Codex · GPT-5.4 Mini`, `OpenAI Codex · GPT-5.3 Codex Spark`, and `Anthropic Claude · Haiku 4.5`
  - `anthropic-claude-haiku-4-5` is a provider-owned engine: the shared translation package only carries its chunk profile, the runtime service lives beside the Claude provider (`packages/Claude_Module/src/translation/claude-haiku-translation-service.ts`), and Core composes the translation facade via `createCoreTranslationFacade(...)` so live reasoning overlays and localization materialization both go through the same Core-backed engine catalog; extension-host must fetch the authoritative Core bootstrap for this engine and fail on missing/mismatched snapshots instead of downgrading to local strict sync
  - Codex GPT translation ids (`codex-gpt-5.4-mini`, `codex-gpt-5.3-codex-spark`) are now provider-owned in Core: `CodexAppServerTranslationEngine` overrides the shared `codex exec` entries in the Core registry and calls `CodexAppServerTranslationService` with `approvalPolicy=never`, `sandbox=read-only`, `persistExtendedHistory=false`, `effort=low`, `summary=none` for non-Spark, and omitted summary for Spark; the shared `codex exec` engine stays as the wrapper fallback during migration
  - the Codex card now surfaces `Reasoning in dialog`, which maps to persisted `reasoningSummaryEnabled`, provider-home `model_reasoning_summary = auto|none`, and a non-Spark live app-server turn policy `summary = detailed|none` resolved from the same shared settings snapshot; this split is intentional because `turn/start.summary = "detailed"` can override provider-home `none`. `gpt-5.3-codex-spark` is the exception: CodeAI Hub omits explicit `turn/start.summary` for Spark and relies on provider-home `model_reasoning_summary`. The Claude card surfaces `Thinking in dialog` as a visible assistant-bubble gate, and the Gemini card keeps the same short `Thinking in dialog` copy as a presentation-only control; for Claude and Gemini the runtime still persists thinking history, while the Session UI decides whether to render it
- General Settings response mode UI: `src/client/ui/src/components/settings/general-response-mode/`
- user-facing PM settings contract exposes only hard recovery `Restart Core`; runtime bootstrap authority still belongs to PM startup path and Core lifecycle, while deeper attach/start controls remain outside Settings UI and the extension settings surface stays compat-only
- Provider modules: `packages/Claude_Module/`, `packages/Codex_AppServer_Module/`, `packages/Gemini_Module/`
- Provider-native request capture cluster: `packages/core/src/provider-network-capture/`
  - `native-request-capture-facade.ts` = Core command façade; maps public Settings commands (`claude`, `codex`, selected `modelId`, optional scenario metadata/prompt) to runtime provider adapters, resolves the same Core-applied turn config used by normal sends, selects a real workflow-agent or translation model invocation profile instead of a synthetic diagnostic profile, prepares TLS credentials, starts the proxy, invokes adapter `captureNativeRequest(...)`, and returns artifact paths
  - `native-request-capture-proxy.ts` = local `127.0.0.1` CONNECT proxy; only target model-request rules count as success (`api.anthropic.com/v1/messages` for Claude, `chatgpt.com/backend-api/codex/responses` for Codex); captured requests are aborted locally instead of being forwarded upstream; Codex WebSocket upgrade requests are completed locally and client frames are delegated to the WebSocket session helper instead of treating the first frame as final
  - `native-request-capture-websocket.ts` + `native-request-capture-websocket-session.ts` = WebSocket upgrade accept, masked client-frame parser, and multi-frame capture session used only by diagnostic capture, because Codex App Server can send an early empty service frame before the real turn payload; Codex capture completes on a useful frame with non-empty `input` / non-`generate:false`, or settles with the best observed frame
  - `native-request-capture-writer.ts` + `native-request-capture-markdown.ts` + `native-request-capture-redaction.ts` = append `.jsonl` and readable `.md` artifacts under `~/.codeai-hub/logs/native-request-capture/` while redacting credential-bearing headers and obvious token fields; `capture_start` records the selected model, workflow scenario metadata and resolved applied config, Markdown lists every matched provider request, chooses the useful Codex full-turn frame as primary when present, extracts Claude `system` / `messages` and Codex `instructions` / `input`, writes provider-supplied diagnostic context such as Codex app-server `thread/start` and `turn/start` payloads into a separate `Provider Diagnostic Context` section, and ignored provider requests preserve reason/target/method/path/redacted headers/body so failed captures still show what client traffic was observed
  - `native-request-capture-certificates.ts` + `native-request-capture-preflight.ts` = local CA/host certificate preparation via `openssl` and preflight failure reasons for missing TLS tooling
- Core-owned session translation overlay cluster: `packages/core/src/session-translation/`, `packages/core/src/unified-session/storage.ts`, `packages/unified-session/src/session-translation-overlay-store.ts`
  - providers now emit source-first thinking/reasoning with stable `messageId`, and Core owns async translation scheduling, persistence, and live bridge patching (`session:message_translation`, `dialog:message_translation`)
  - canonical session/dialog transcript stays native-only; translated text is stored separately in per-session `*.translations.jsonl` overlays and merged back into history as `localizedContent` only when `messageId + sourceHash` still match
  - UI renders `localizedContent ?? content`, so late translation completion upgrades already-visible messages in place without delaying the original thinking stream
  - live overlay translation now resolves settings per dispatch, serializes execution through one shared worker queue, deduplicates identical in-flight jobs by `engineId + targetLanguage + sourceHash`, and refuses to start while the persisted localization bootstrap snapshot does not match current settings
- Claude messaging cluster: `packages/Claude_Module/src/messaging/`
  - `message-processor.ts` = thin façade / queue orchestration surface; shutdown-aware error suppression so `Stop` interrupts cannot escape as `ERR_UNHANDLED_ERROR` after the session error channel has been torn down
  - `claude-stream-event-router.ts` = assistant/result/structured-output/thinking/text routing; delegates the live content ingestion path to `claude-content-stream-handler.ts` and reconciles final assembled thinking AND text blocks against materialized live segments before emitting
  - `claude-content-stream-handler.ts` = micro-class watching `content_block_start/delta/stop` for thinking and text blocks; owns both live buffers and exposes `consumeFinalThinking` / `consumeFinalText` / `hasMaterializedText`
  - `claude-thinking-live-buffer.ts` = per-session accumulator for `thinking_delta` fragments (flush threshold 240 chars); surfaces reasoning live before the assembled thinking block arrives
  - `claude-text-live-buffer.ts` = per-session accumulator for `text_delta` fragments (flush threshold ~96 chars); eliminates the multi-minute pending silence on pre-tool assistant text while Claude streams large `input_json_delta` for `Write`/`Edit`
  - `claude-structured-output-helpers.ts` = structured output parsing/partition/normalization helpers split out of the router to keep it under the 500-line architecture limit
  - `claude-thinking-dialog-emitter.ts` = emits thinking dialog bubbles (`emitClaudeThinkingDialog`) and live assistant text bubbles (`emitClaudeAssistantLiveText`) as append-only messages with unique uuid suffixes so Core translation overlays can attach per-bubble `localizedContent`
  - `claude-message-finish-handler.ts` = turn lifecycle completion façade
  - `claude-usage-sync.ts`, `claude-token-usage-sync.ts` = usage-limits/context-token synchronization internals
- Codex app-server cluster: `packages/Codex_AppServer_Module/src/app-server/`
  - `codex-app-server-facade.ts` = thin transport façade over `thread/start`, `thread/resume`, `turn/start`, `turn/interrupt`, per-thread active turn tracking, and session listener fan-out
  - `process/codex-app-server-process.ts` = long-lived stdio/JSON-RPC bridge; owns `initialize` handshake with `experimentalApi`, provider-home bootstrap, request correlation, and process lifecycle
  - `process/codex-provider-home-config.ts` = runtime provider-home `config.toml` summary materializer; before App Server startup it removes legacy `default_reasoning_summary` and writes `model_reasoning_summary = "auto" | "none"` from shared Codex settings
  - `codex-app-server-event-router.ts` = notification normalization for `turn/started`, `turn/completed`, `item/agentMessage/delta`, reasoning summaries, token usage, and usage-limit snapshots
  - `packages/Codex_AppServer_Module/src/translation/` = provider-owned Codex App Server translation path; `codex-translation-prompt-profile.ts` owns translation system/user prompt construction and `codex-app-server-translation-service.ts` owns isolated temporary workspace, translation process profile, strict thread/turn params, and cleanup
  - `packages/Codex_AppServer_Module/src/diagnostics/codex-native-translation-capture-profile.ts` = fixed small translation sample plus translation `thread/start` / `turn/start` payload builders used only by native request capture scenario `Translation`
  - `packages/Codex_AppServer_Module/src/provider/codex-provider-adapter.ts` = Core-facing `ProviderAdapter` bridge over the app-server façade and usage-limit refresh path
  - app-server `thread/start` + `thread/resume` return a real `threadId`, so Codex immediate binding is provider-native and no legacy temp-session seam is required
  - `turn/start` keeps Core-owned effective model identity, reasoning effort, and optional `outputSchema` passthrough on the outbound turn envelope
  - active provider diagnostics do not use always-on provider SDK/raw file-backed logs under `~/.codeai-hub/logs/{claude,codex,gemini}`; the remaining runtime evidence layers are session-local normalized history, provider-native provider-home artifacts, and optional native request capture artifacts
- Gemini messaging cluster: `packages/Gemini_Module/src/messaging/`
  - `message-processor.ts` = thin façade / turn event normalization entrypoint
  - `gemini-stream-event-router.ts` = event dispatch and stream-error handling
  - `gemini-assistant-event-normalizer.ts` = assistant/thinking/finished boundary normalization
  - `gemini-system-event-normalizer.ts` = tool/system/warning event normalization
  - `packages/Gemini_Module/src/logging/session-logger.ts` + `gemini-system-event-normalizer.ts` + `gemini-assistant-event-normalizer.ts` = сохраняют raw/diagnostic Gemini session artifacts; active baseline не промотирует `model_info`, `thought` или `thinkingLevel` в отдельный normalized provider-feedback contract; thinking text is emitted source-first and any translation now arrives later through the Core overlay path
- Gemini session façade cluster: `packages/Gemini_Module/src/session/`
  - `gemini-session-manager.ts` = façade
  - `gemini-session-bootstrapper.ts`, `gemini-session-settings-resolver.ts`, `gemini-session-store.ts`, `gemini-session-lifecycle.ts`, `gemini-turn-runner.ts`, `gemini-tool-call-orchestrator.ts` = runtime internals; bootstrap/lifecycle now keep a mutable `runtimeTurnConfig` so Core-applied model/thinking changes can retune existing Gemini sessions without re-deriving model/thinking authority from local provider settings; `gemini-turn-runner.ts` treats assistant output from tool-producing legs as progress-only output, drains deferred Gemini dialog emits before detaching segment accounting listeners, and `gemini-session-lifecycle.ts` differentiates `initial` and `post_tool` stalled watchdog windows for Gemini follow-up legs
- Gemini provider send path: `packages/Gemini_Module/src/provider/gemini-provider-adapter.ts`
  - `gemini-applied-turn-config.ts` = reads Core-applied next-turn effective model identity for Gemini sends and stages runtime overrides before provider execution
  - `gemini-provider-adapter.ts` = consumes the shared Core-applied runtime envelope on outbound send; `gemini-session-settings-resolver.ts` now treats Core-provided model/thinking as authoritative over local snapshot values, leaving `settings.json` only as fallback for continuity/runtime defaults that are not part of the applied turn contract; stalled-turn watchdog failures are surfaced as provider `turn_failed` events and kept on the recoverable session path instead of escalating through generic provider-runtime failure recovery, while non-thinking assistant text from a leg that still emitted tool calls is no longer accepted as whole-turn completion proof
- Claude SDK send path: `packages/Claude_Module/src/sdk/claude-sdk-manager.ts`
  - `claude-sdk-manager.ts` derives the active turn model from Core-applied turn config on send path; `handlers/session-request-handler-applied-turn-config.ts` resolves Claude `defaultModel` from the shared persisted settings snapshot before outbound send, so Claude no longer falls back to a stale process-start env alias when Settings change during a live Core session
  - CodeAI Hub-managed Claude turns now keep filesystem `settingSources` empty, which places the provider in full SDK isolation mode and blocks parent-directory `CLAUDE.md` / settings discovery from the active workspace path
  - visible Claude thinking is now emitted source-first with stable ids and translated later through the Core overlay pipeline; `claude-thought-translation-adapter.ts` remains only for short generic assistant progress/pre-tool text that stays provider-local
  - short assistant progress text that belongs to a Claude message ending with `stream_event.message_delta.delta.stop_reason = "tool_use"` is still buffered and localized on the user-facing path, while ordinary final assistant replies that end with `end_turn` stay on the normal untranslated assistant-output path
  - Claude live reasoning/text chunking is marker-safe for markdown lists: provider-side live buffers are not allowed to flush a visible fragment whose tail is only a list marker line (`1.`, `2.`, `-`, `*`, `+`) without the item body; they must backtrack to the previous safe boundary or keep buffering until more text arrives / terminal flush
  - Claude thinking settings now use `thinking.enabled + effort`; Core includes `thinkingEnabled` and `reasoningEffort` in applied turn config, while the client sees enabled Claude effort through effective identities such as `sonnet reasoning:high` rather than only `thinking:on`
  - current Claude SDK semantics for `claude-opus-4-6` still keep final thought-summary verbosity provider-owned even when CodeAI Hub sends explicit `effort`, so short visible thought summaries in provider-home JSONL are not a UI truncation bug by themselves
  - provider-owned Claude Haiku translation queries reuse the same auth/bootstrap path, run under dedicated slug `translation-runtime-haiku`, and now persist native provider JSONL under the Claude provider-home for audit/debug forensics
- CEF Launcher native boundary: `packages/cef-launcher/src/launcher_handler.cc`
  - `launcher_handler_bridge_helpers.h` owns URL classification, bridge injection, data-URI creation, and browser payload serialization; `launcher_handler.cc` stays on lifecycle/orchestration, while platform-specific title/show/persist behavior remains in `platform/*/launcher_handler_*`.
  - `packages/cef-launcher/src/platform/mac/codeai_hub_application_mac.{h,mm}` own the macOS-specific CEF application shell (`NSApplication <CefAppProtocol>`, delegate-driven quit/reopen hooks, secure restorable state); `packages/cef-launcher/src/platform/mac/app_main_mac.mm` stays a thin browser-process entrypoint and must not inline that lifecycle logic again.
- Project Manager applied-config sync:
  - `src/client/project-manager/components/sessions/use-runtime-model-sync.ts` = session label updates only from Core-confirmed runtime effective model identity events, including reasoning refresh for same-base-model turns
  - `src/client/ui/src/app-host/use-settings-models-sync.ts` = ready sessions no longer guess a new runtime identity from settings before Core confirms the applied effective model config
  - `packages/core/src/remote-bridge/handlers/session-request-handler-message-dispatch.ts` now emits `session:model:update` from the outbound applied turn-config itself for regular new turns, so PM label sync does not depend on a provider-specific `model_info` or `system` event being emitted afterward and does not reconstruct identity from split fields on the UI side
  - Session dialog markdown merge/render ownership is split by layer: provider/runtime must not emit marker-only live fragments, `mergeThinkingMessages` in the shared Session UI repairs any already-split thinking list boundary (`2.` + next fragment text) and now preserves one blank paragraph boundary before the next standalone bold heading block, while PM dialog CSS keeps the heading/body rhythm by removing the extra gap only for paragraph/list siblings that immediately follow a bold-only heading paragraph
- Codex response policy outbound bridge: `packages/core/src/remote-bridge/handlers/session-request-handler-applied-turn-config.ts`, `packages/Codex_AppServer_Module/src/provider/codex-provider-adapter.ts`
- Codex visible thinking / reasoning summaries: `packages/Codex_AppServer_Module/src/app-server/codex-app-server-event-router.ts`, `packages/core/src/session-translation/`
  - app-server `item/completed` for `agentMessage` preserves phase separation on the normalized user-facing path: `phase: "commentary"` materializes as non-terminal `dialog_message` with `role: "assistant"` and `tag: "commentary"`, while `phase: "final_answer"` remains the terminal assistant answer
  - reasoning summaries are emitted source-first from app-server completed summary blocks and translated asynchronously through the Core overlay pipeline;
  - Core threads `messagesForTheUserLanguage` from the shared settings snapshot into the overlay translator, so visible reasoning follows the selected `Messages for the User` language while the native transcript remains unchanged;
  - visible output still uses `role: "assistant"` with `tag: "thinking"` whenever upstream Codex actually sends reasoning summaries;
  - non-Spark live app-server turns resolve `summary = "detailed" | "none"` from the shared settings snapshot; `detailed` is the baseline required for completed reasoning summary blocks, while `none` suppresses the reasoning stream entirely;
  - `gpt-5.3-codex-spark` omits explicit turn-level `summary` because Spark rejects provider-native `reasoning.summary`; its readable-summary control is provider-home `model_reasoning_summary = "auto" | "none"`;
  - app-server `summaryTextDelta` / `textDelta` remain provider diagnostics and fallback inputs, but provider normalization no longer emits user-facing live token/sentence fragments; visible reasoning is materialized only on `item/completed`, using `item.summary[]` first and accumulated summary/content/text only as fallback;
  - provider-home `model_reasoning_summary = "auto" | "none"` remains persisted compatibility state for non-Spark models and the supported readable-summary path for Spark;
  - the provider settings toggle updates provider-home `config.toml` immediately, and process startup re-materializes the same setting before `codex app-server` so saved settings remain the restart-proof source of truth;
  - app-server transport diagnostics are append-safe/rotate-safe per process start and remain diagnostic-only; they complement, but do not replace, normalized dialog/session history or provider-home artifacts;
  - token usage and usage-limit notifications now travel through the same normalized app-server event surface as dialog messages.
- Gemini visible thinking: `packages/Gemini_Module/src/messaging/gemini-assistant-event-normalizer.ts`, `packages/core/src/session-translation/`
  - Gemini thought text is emitted source-first, then optionally upgraded through the Core overlay translator; `en` skips translation and preserves upstream provider wording
  - visible output remains `role: "assistant"` with `tag: "thinking"` — UI renders as "Gemini · Thinking" when `thinkingDisplaySyncEnabled` is on; when the display flag is off, the Session UI filters the bubble while runtime history/logging path remains intact
  - graceful degradation is now overlay-based: on failure, the original English thought remains visible because the source transcript was already persisted first
  - Канон: `doc/SolidWorks-WorkFlow/Contracts/Gemini_ThoughtTranslation.md`, `packages/translation/src/translation-facade.ts`

## 5) Workflow Boundary (Description, 2026-03-01)

- Шаг `description` зафиксирован как single-agent file-first flow:
  - pre-submit: `questionnaire.md` + user Help,
  - post-submit: единая resume-сессия,
  - SSOT-артефакт: `Final_Description.md`.
- `Final_Description.md` должен формироваться сразу после чтения анкеты (первичный черновик), чтобы пользователь обсуждал уже существующий документ.
- `Final_Description.md` должен содержать не только описание идеи, но и базу для следующего шага `virtual_simulation`:
  - ключевые сценарии в количестве, достаточном для покрытия продукта (актор/цель → действие → ожидаемый результат → критерий успеха),
  - ограничения/допущения,
  - ключевые сущности/термины.
- Product-visible contract шага не использует `description.md`, manual restart или встроенный reviewer; internal compat для legacy draft допустим только как non-SSOT fallback.
- Legacy naming `Idea` / `Idea Collector` больше не участвует в PM bootstrap, user-facing copy, release packaging или stage semantics текущего workflow. Если такие имена ещё встречаются в коде, они допускаются только как internal helper alias, provider parsing internals, archived/deferred flow leftovers или redirect-only compat слой.
- Standalone Reviewer вынесен в deferred-модуль и не входит в базовый workflow 1→6.

## 6) Runtime Templates Boundary (Description + Virtual Simulation)

Каноничные bundled templates в `.codeai-hub/templates/description/`:
- `questionnaire-template.md` — pre-submit анкета.
- `description-template.md` — runtime/reference copy Description Help; PM не должен требовать этот файл для локального рендера Help.
- `description-collector-prompt.md` — инструкции Description Agent (file-first, краткий контекст workflow, ограничения, DoD).

Инвариант delivery для `Description`: user-facing PM Help не должен зависеть от наличия `description-template.md` на диске или от ответа `description-contract`. Runtime может синхронизировать и восстанавливать `description-template.md` как reference asset для workflow contract, но кнопка `Help` в PM обязана рендериться локально по тому же паттерну, что и остальные step helps.

Каноничный bundled prompt для `.codeai-hub/templates/virtual_simulation/`:
- `virtual-simulation-prompt.md` — инструкции Virtual Simulation Agent.

Инвариант: `Virtual Simulation` работает в режиме prompt-only. Отдельный artifact template (`virtual-simulation-template.md`) в runtime не поставляется и не отправляется агенту.

Канонические документы:
- `doc/SolidWorks-WorkFlow/Contracts/DescriptionStep_SingleAgent.md`
- `doc/SolidWorks-WorkFlow/System/WorkflowSteps_Overview.md`
- `doc/SolidWorks-WorkFlow/Contracts/Workflow_CLI.md`

## 6.1) Diagram DSL Artifact Boundary (Phase 1, 2026-03-16; updated Phase 57, 2026-03-24)

- Workflow шаг `Diagram Modules` больше не использует Mermaid `.mmd` как SSOT.
- Канонические semantic artifacts:
  - `.codeai-hub/<workspaceSlug>/diagram_modules/product-parts.index.md`
  - `.codeai-hub/<workspaceSlug>/diagram_modules/product-parts/<part-id>.md`
- Канонические auxiliary artifacts:
  - `*.flow.json` для layout/view state.
- Agent instructions и templates для этого шага поставляются из `packages/agents/diagram-modules-agent/assets/`, а не из `packages/core/src/templates/source/*.mmd`.
- Runtime обязан считать `.md` artifact единственным product-visible SSOT, а `*.flow.json` трактовать как non-semantic sidecar.
- Facade specs are deferred to per-cluster and per-module branches (future work) and are not a trunk workflow step.

Канонические документы:
- `doc/SolidWorks-WorkFlow/Plans/Archive/DiagramWorkflow_UserSurface_Architecture.md`
- `doc/SolidWorks-WorkFlow/System/WorkflowSteps_Overview.md`
- `doc/SolidWorks-WorkFlow/Contracts/Workflow_CLI.md`

## 6.2) Diagram Visual Shell Boundary (Phase 2, 2026-03-16; updated 2026-04-09 for Sidecar v2 layoutParams)

- Project Manager для `Diagram Modules` рендерит канонические DSL artifacts через diagram-first visual shell на базе CSS Grid (React Flow удалён в релизе `1.1.921`).
- Правый panel contract для diagram stage = `Artifacts | Help` (Source mode убран):
  - `Artifacts` показывает саму диаграмму;
  - `Help` показывает guidance по шагу.
- Visual shell не владеет semantic state:
  - source of truth остаётся `product-parts.index.md` + `product-parts/<part-id>.md`;
  - shell работает как projection layer `Markdown DSL -> domain model -> nested CSS Grid`, но владеет только layout/view state.
- `*.flow.json` остаётся non-semantic sidecar:
  - формат v2 хранит опциональную секцию `layoutParams` (`productParts` + `clusters`) с declarative CSS Grid overrides, помимо placeholder positions и viewport;
  - parser принимает payload'ы обеих версий (`v1` без `layoutParams` и `v2` с ними); невалидные enum-значения `columns`/`targetAspectRatio`/`moduleColumns` отбрасываются entry-by-entry и возвращаются к defaults;
  - пишется отдельно через `workspace-file-write`;
  - не меняет содержимое канонического `.md`;
  - не показывается пользователю как primary artifact.
- Если sidecar отсутствует, не совпадает по `Revision` или относится к более ранней версии формата, shell обязан взять стартовую CSS Grid композицию из собственной domain projection. Пользовательские CSS Grid overrides, сохранённые в `layoutParams`, применяются поверх defaults через `applyFlowSidecarLayoutParams` в read-path.
- Product contract для diagram layout теперь `CSS Grid declarative first`:
  - AI/DSL задаёт semantic structure диаграммы;
  - декларативные CSS Grid параметры (`columns`, `targetAspectRatio` на ProductPart; `moduleColumns` на Cluster) принадлежат `*.flow.json` v2 и управляются через right-click context menu;
  - пользователь не таскает ноды мышью — permanent composition состояние выражается через layout params, а не через pixel-positions.
- Browser/UI bundle не должен зависеть от Node-only imports ради рендера diagram artifacts; для `Revision` browser-safe parsing path может переиспользовать уже записанное поле `- Revision:` из канонического Markdown DSL.

Канонические документы:
- `doc/SolidWorks-WorkFlow/Contracts/Workflow_CLI.md`
- `doc/SolidWorks-WorkFlow/Contracts/ProjectManager_WorkflowNavigation_SSOT.md`
- `doc/SolidWorks-WorkFlow/Clusters/Project_Manager.md`

## 6.3) Module Semantic Source Boundary (Phase 3, updated Phase 57, 2026-03-24)

- `Diagram Modules` keeps semantic truth in staged Markdown artifacts, not in the visible graph canvas.
- Видимый UI больше не содержит inline-редакторов для module entities и relations.
- `product-parts.index.md` is the first canonical orchestration artifact for `Diagram Modules`.
- `product-parts/<part-id>.md` are the primary semantic artifacts of individual `Product Part`.
- The Module Graph is built progressively from individual `product-parts/<part-id>.md` files; no single aggregate file is generated.
- Начиная с ownership-aware migration (`2026-03-21`), canonical inventory model для `Diagram Modules` включает явный верхний уровень `Product Part -> Cluster -> Module`.
- Parser/runtime обязаны поддерживать dual-read migration path: новый hierarchical DSL читает explicit `Product Parts`, а legacy flat inventories временно materialize synthetic `default-product-part`, чтобы старые workspace artifacts оставались parseable без ручной миграции.
- Начиная с cleanup wave `2026-03-28`, `diagram-modules-parser.ts` больше не является god-parser: root file сведен к thin orchestration surface, а relation/module/cluster/product-part/legacy ownership parsing вынесены в отдельные focused helpers.
- Graph canvas continues to allow manual layout edits, and those changes remain in `module-map.flow.json` only.
- Provenance and merge handling stay in the agent/runtime path, not in the visible surface.

Канонические документы:
- `doc/SolidWorks-WorkFlow/Contracts/Workflow_CLI.md`
- `doc/SolidWorks-WorkFlow/Clusters/Project_Manager.md`

## 6.4) Diagram Workflow Stabilization Boundary (Phase 5, 2026-03-16; updated Phase 57, 2026-03-24)

- Markdown DSL runtime обязан быть tolerant к platform-level text variance:
  - parser принимает UTF-8 BOM и CRLF line endings;
  - serializer нормализует multiline semantic blocks к canonical LF output.
- Shared diagram editor UX обязан сохранять визуальную непрерывность:
  - background refresh не должен очищать уже загруженный graph перед следующим успешным parse/load;
  - empty semantic graph обязан показывать explicit placeholder вместо silent blank canvas;
  - layout param changes (right-click context menu) обязаны обновлять CSS Grid рендер в реальном времени и сохраняться в `*.flow.json` v2 `layoutParams`;
  - visual shell не показывает auto-layout chrome, zoom/fit controls или minimap; зум выполняется через CSS `transform` по Cmd/Ctrl+scroll (25–200%, шаг 1%), reset — Cmd/Ctrl+0 или clickable badge в status bar; drag-based node movement отсутствует — композиция управляется декларативными layout params. Дополнительно (релиз `1.2.40`, уточнено в `1.2.41`): диаграмма авто-подгоняется по ширине Artifacts panel через `ResizeObserver` + natural `scrollWidth` композиции — effective scale = `autoFitScale × userZoom`, где `autoFitScale = min(1, containerWidth / naturalWidth)` с нижним порогом `0.25`. Manual Cmd/Ctrl+scroll — overlay поверх auto-fit базы; Cmd/Ctrl+0 сбрасывает только user overlay, не auto-fit. Inner composition обязана оставаться в естественном grid sizing режиме — без intrinsic-sizing keyword'ов (`width: max-content` и т.п.) на самом grid-container'е: `scrollWidth` на нормально-сайзнутом grid уже равен `max(clientWidth, rightmost-child.right)` и поднимается выше container width ровно тогда, когда реальные cluster/module card constraints (`minWidth: 220`, `minmax(240px, 1fr)` в ProductPart header) выталкивают content за track. Попытка форсировать `width: max-content` на composition-container'е разрешает prose (purpose text, long titles) и `1fr` column tracks развернуться в unwrappable single lines, что раздувает natural width в тысячи пикселей и schleppt auto-fit в floor независимо от user zoom.
- Workflow tree child nodes для `Diagram Modules` обязаны наследовать актуальные stage-level `blocked/outdated` сигналы; поддеревья диаграмм не могут маскировать реальный gating state как постоянный `active`.
- Stage activation для шага `Diagram Modules` обязано следовать тому же product contract, что и `Description -> Virtual Simulation`: если upstream canonical artifact уже существует, PM обязан разрешить ручной запуск следующего шага без дополнительного требования `upstream stage === completed` и без превращения `invalid/outdated` статуса upstream stage в hard blocker. Эти статусы остаются диагностическими, но не отменяют user-driven переход на следующий шаг. Навигация осуществляется исключительно через sidebar Workflow Tree (верхний toolbar удалён в v1.1.924).
- `WorkflowState` на cold start не может зависеть только от watcher-memory. При чтении `/workflow-state` Core обязан гидрировать canonical artifacts (`Final_Description.md`, `virtual-simulation.md`, `product-parts.index.md`) с диска, чтобы gating и stage snapshot оставались корректными после перезапуска Core / Project Manager.
- Diagram workflow contract не может ограничиваться только base prompt и template path. Для `diagram_modules` runtime обязан сначала читать strict field-reference и merge-rules из synced visible templates под `~/.codeai-hub/templates/...`, а package assets использовать только как bundled-source fallback, чтобы генерируемый Markdown DSL не изобретал невалидные enum values и оставался parseable для visual shell.
- Module table contract в staged `product-parts/<part-id>.md` — канонический 2-колоночный: `| \`module-id\` | Responsibility |`. После удаления `ModuleKind` (релиз `c488df065`) ни один парсер не имеет права требовать дополнительную backtick-обёрнутую колонку между id и responsibility; display title модуля humanize-ится из id. `development-tree-snapshot.ts` обязан ограничивать body секции `## Standalone Modules` следующим `##`-заголовком, чтобы строки `## Simple Relations` (`| \`from-id\` | \`to-id\` | type | label |`) не утекали как фантомные standalone modules. Это требование покрывает оба reader'а артефакта: browser staged-part parser (`diagram-modules-staged-part-parser.ts`) для diagram canvas и Core `development-tree-snapshot.ts` для PM sidebar. Regex lastIndex safety (релиз `1.2.40`): module-level `/g`-regex в этом парсере не могут потребляться через прямой `.exec()` или `.test()` — их `lastIndex` сохраняется между вызовами внутри long-lived Core процесса и даёт чередование hit/null на одном и том же артефакте, из-за чего standalone clamp теряется и sidebar периодически показывает фантомные standalone modules. Допустимые способы потребления: `.matchAll()` (iterator не использует shared lastIndex), `.search()` на non-global regex, а также factory-функции, возвращающие свежий regex-инстанс на каждый вызов. Плюс `MODULE_ROW_RE` обязан быть строго 2-column (`[^|\n]+` во второй колонке + якорь `\|[ \t]*$`), чтобы 4-колоночные Simple Relations rows физически не матчились даже если clamp когда-либо опять соскользнёт.
- Для empty-workspace greenfield сессий agent source boundary обязан оставаться project-local: `Virtual Simulation` и `Diagram Modules` читают только canonical artifacts текущего проекта внутри `.codeai-hub/<workspaceSlug>/...`, continuity files текущего stage и файлы, которые пользователь явно назвал для этого проекта. Исходники CodeAI Hub, parser/runtime implementation и чужие repo-level docs не могут выступать источником архитектурных решений для artifact generation.
- Для `Diagram Modules` semantic runtime contract теперь staged:
  - first artifact: `product-parts.index.md`;
  - primary semantic part artifacts: `product-parts/<part-id>.md`.
- Trunk workflow now ends at `Diagram Modules`. After diagram approval, work continues as a Development Tree `[DESIGNED, NOT IMPLEMENTED]`:
  - **Product Part Specification** (per each part from Module Graph) becomes the branch-root artifact and fixes role, boundaries, owned clusters, standalone modules, and first implementation waves for that part.
  - **Cluster Design** (per each cluster inside part) is one design-step with two outputs:
    - Cluster Specification (functions, constituent modules, cluster-level responsibility);
    - Cluster Facade Contract (external contract of the cluster).
  - **Module Design** (per each module inside a cluster) is one design-step with two outputs:
    - Module Specification (interfaces, methods, dependencies);
    - Module Facade Contract (public API).
  - **Standalone Module Design** uses the same two-artifact pattern for modules that belong to a `Product Part` but do not belong to any cluster.
  - `Implementation Foundation` starts only after the selected implementation wave has approved branch artifacts and required seam/shared contracts, then hands off to TODO Plan and code implementation.
  - Facades are NOT a separate trunk step; they remain separate artifacts, but for clusters and modules they are designed in the same session as the matching specification.
- Diagram workflow user surface не может подменять диаграмму raw Markdown source по умолчанию. При reopen/resume `Diagram Modules` Project Manager обязан возвращать пользователя в `Artifacts` (visual diagram), а `Source` оставлять вторичным debug view.
- Пока canonical artifact ещё не создан, `Artifacts` panel для workflow stage обязан показывать тот же help-content, что и вкладка `Help`; отдельный pending-intro prose вне help SSOT не допускается.
- Ordinary dialog reopen/recovery contract обязан сохранять identity continuity между PM, Core continuity и provider runtime. Если runtime по любой причине создает fresh provider session вместо обычного resume, новый binding должен быть immediately normalized в continuity/index до следующего outbound user turn, а PM не имеет права бесконечно повторять `createSession(old providerSessionId)` для того же continuity entry.
- `Diagram Modules` не навязывает пользователю inline semantic editors или bottom-right minimap. Product UX обязан опираться на:
  - AI-generated semantic structure в canonical `.md`;
  - nested ownership containers для `Diagram Modules`, где `Product Part` = top-level CSS Grid контейнер, `Cluster` = nested CSS Grid контейнер, `Module` = child card внутри cluster или напрямую внутри owning product part;
  - right-click context menu для decalarative layout params (columns, aspect ratio, module columns), результат которых сохраняется в `module-map.flow.json` v2 `layoutParams`;
  - agent-driven semantic updates when new semantic content is needed.
- Diagram canvas interaction model (начиная с релиза `1.1.922`, Sidecar v2):
  - **Right-click context menu** на ProductPart: `columns` (`auto` | 2..5), `targetAspectRatio` (`landscape` | `wide` | `square`); на Cluster: `moduleColumns` (`auto` | 1..3). Выбранные значения мутируют in-memory nodes, сразу уходят в sidecar v2 через `onNodesChange`, и переживают reload через `applyFlowSidecarLayoutParams` на read-path;
  - **Zoom**: CSS `transform: scale(...)` по Cmd/Ctrl+scroll (range 25–200%, шаг 1%); reset — Cmd/Ctrl+0 или clickable badge в bottom-left при scale ≠ 100%;
  - **Без drag-based node movement**: CSS Grid сам раскладывает card'ы согласно layout params. Нет collision avoidance/containerConstraints/resizeContainersToFit — всё это реализует браузер.
- Detachable diagram window:
  - кнопка `Detach` в artifact header (слева от `Artifacts` toggle) открывает popup-sized CSS Grid view в отдельном CEF popup через `window.open()`;
  - detached окно использует тот же sidecar файл (`module-map.flow.json`), что и основной PM — layout params v2 синхронизированы;
  - `BroadcastChannel("pm:diagram:sidecar-sync")` уведомляет второе окно о перезагрузке sidecar после write;
  - detached popup не является owner-window приложения: его закрытие не должно завершать main PM window и не должно reuse-ить autosaved frame главного PM окна;
  - реализация: `detached-diagram-view.tsx`, `detach-diagram-button.tsx`, `stage-artifact-header-toggle.tsx` (`extraActions` slot).
- Workspace auto-select (обновлено в 1.1.931): при открытии workspace sidebar автоматически выбирает последний non-idle stage (diagram_modules → virtual_simulation → description) на основе `resolveLastActiveStage` и `resolveStartupTool`. Трёхцветные индикаторы (gray/orange/green) показывают состояние каждого trunk stage.

Канонические документы:
- `doc/SolidWorks-WorkFlow/Contracts/Workflow_CLI.md`
- `doc/SolidWorks-WorkFlow/Contracts/ProjectManager_WorkflowNavigation_SSOT.md`
- `doc/SolidWorks-WorkFlow/Clusters/Project_Manager.md`

## 6.5) Diagram Modules Ownership Hierarchy Boundary (Phase 6, 2026-03-21; updated 2026-04-09 post React Flow removal and Sidecar v2)

- `Diagram Modules` больше не ограничивается semantic baseline вида `cluster + standalone module`.
- Канонический semantic runtime contract для module stage теперь включает `ProductPartEntity[]`, `ClusterEntity[]`, `ModuleEntity[]` и `ModuleRelation[]`.
- `Product Part` является верхним ownership layer для user-facing diagram:
  - имеет `title`, `purpose`, ordered `clusterIds`, ordered `standaloneModuleIds`;
  - не может быть пустым;
  - не может использоваться как relation endpoint.
- `Cluster` обязан принадлежать ровно одному `Product Part`, а `Module` обязан принадлежать ровно одному `Product Part` и максимум одному `Cluster`.
- Visual shell projection для `Diagram Modules` обязана использовать nested CSS Grid container model:
  - `Product Part` = top-level CSS Grid контейнер, положение которого задаётся декларативными layout params (`columns`, `targetAspectRatio`);
  - `Cluster` = nested CSS Grid контейнер внутри owning product part, чья внутренняя раскладка задаётся per-cluster `moduleColumns`;
  - `Module` = child card внутри cluster или напрямую внутри product part, если модуль standalone;
  - контейнеры не владеют pixel-level constraints (`parentId`, `extent: "parent"`, `containerConstraints`, collision avoidance) — layout driven целиком браузерным CSS Grid движком, а не JS measure/place pass (см. §6.2, §6.4).
- First-open composition для ownership-aware `Diagram Modules` обязан оставаться readable без user drag:
  - top-level `Product Part` containers раскладываются как независимые lane sections и не могут overlap друг с другом;
  - standalone modules внутри `Product Part` компактизируются через тот же owning-grid, а не через отдельный auto-layout pass;
  - ownership-free external modules/boundaries (например выбранный AI provider) визуализируются вне product-part container, а не как внутренние элементы его ownership layer.
- Начиная с workflow cleanup baseline (`2026-04-07`), `Diagram Modules` фиксируется как главный user-feedback checkpoint и одновременно как последний шаг ствола:
  - пользователь именно здесь впервые видит архитектуру в наглядной форме и должен иметь возможность активно её корректировать;
  - после `Diagram Modules` workflow сразу переходит к branch-level design начиная с `Product Part Specification`;
  - facade specs are deferred to per-cluster and per-module branches (future work).
- Начиная с product-part decomposition baseline (`2026-03-23`), `Diagram Modules` больше не должен упираться в giant single-turn generation:
  - сначала runtime materialize-ит `product-parts.index.md`;
  - затем отдельные `Product Part` materialize-ятся по одному;
  - visual shell обязана progressively регенерировать CSS Grid composition по мере появления новых part artifacts;
  - relation lines и cross-part wiring исключаются из первого обязательного baseline slice.
- First-open layout contract для `Diagram Modules` теперь полностью декларативен:
  - runtime отдаёт visual shell только semantic tree (`Product Part -> Cluster -> Module`) и optional layout params из sidecar v2;
  - CSS Grid контейнеры раскладывают child cards нативно — runtime не выполняет JS-based measure/place pass, runtime не владеет header budget'ами и не вычисляет child positions вручную;
  - defaults layout params выбираются projection layer'ом (`columns: auto`, `targetAspectRatio: landscape`, `moduleColumns: auto`), пока пользователь не override-ит их через right-click context menu.
- User-facing hierarchy contract для ownership containers:
  - `Product Part` и `Cluster` обязаны показывать короткий purpose/description surface, а не только title и counters;
  - standalone modules компактизируются через owning product part CSS Grid column track, а не через отдельную band-logic;
  - outer frame `Product Part` замыкается по реально занятому CSS Grid содержимому — без пустой декоративной вертикали.
- `module-map.flow.json` остаётся non-semantic layout sidecar:
  - хранит placeholder positions, viewport и (в формате v2) опциональную секцию `layoutParams` с declarative CSS Grid overrides (`columns`/`targetAspectRatio` на ProductPart, `moduleColumns` на Cluster);
  - не переносит ownership semantics и не дублирует содержимое канонического `.md`;
  - применяется только если `Revision` sidecar совпадает с текущим semantic artifact; если нет, visual shell стартует с projection defaults и игнорирует sidecar (см. §6.2).
- Runtime orchestration contract для `Diagram Modules` использует step-by-step workflow (начиная с 1.1.778):
  - index turn: агент создаёт `product-parts.index.md`, задаёт вопросы по составу, ждёт подтверждения пользователя;
  - part turns: пользователь подтверждает → агент создаёт один `Product Part`, ждёт подтверждения;
  - visual shell автоматически обновляется при каждом новом artifact (`pm:diagram:refresh` event);
  - sidebar label: `Module Graph` (Source mode убран — визуальная композиция является основным артефактом).

Канонические документы:
- `doc/SolidWorks-WorkFlow/Contracts/Workflow_CLI.md`
- `doc/SolidWorks-WorkFlow/Clusters/Project_Manager.md`

## 7) Codex Response Mode Boundary (2026-03-13)

- `Settings -> General` теперь владеет persisted policy `general.responsePolicy`; эта настройка не смешивается с `Core Controls`.
- В Project Manager-owned settings surface `Core Controls` снова видимы как recovery block рядом с persisted response policy: General tab допускает `Restart Core`, но не превращается в полный runtime lifecycle console.
- Baseline default для workflow-сценариев: `hybrid`.
- `strict` оставляет editable schema/instruction contract для узких machine-readable turn-ов.
- `debug_raw` нужен для исследования новых моделей без baseline default schema pressure на обычные turn-ы.
- Provider-home native artifacts and optional native request capture are diagnostic artifacts; dialog/history остаётся нормализованным display-слоем. Always-on provider SDK/raw JSONL mirrors under `~/.codeai-hub/logs/{claude,codex,gemini}` are not part of the runtime contract.
