# SolidWorks-WorkFlow — Docs Index (SSOT навигация)

## 0) Start here (восстановление контекста)

Использование этого индекса:

- если активный `todo-plan.md` уже существует, список документов для текущего execution cycle нужно брать только из него;
- если предыдущий session report закрыл старый план полностью, то после обсуждения нового задания с пользователем агент использует этот индекс, чтобы выбрать релевантные документы для нового planning scope;
- этот индекс является навигационным каталогом, а не инструкцией читать все документы подряд.

1. `System/SystemArchitecture.md`
2. `System/WorkflowSteps_Overview.md`
3. `Clusters/Project_Manager.md`
4. `Clusters/CoreOrchestrator.md`
5. `Modules/Claude.md`, `Modules/Codex.md`, `Modules/Codex_ProviderInvocationFlags.md`, `Modules/Gemini.md`, `Modules/Shared_RuntimeTranslation_Module.md`, `Modules/Localization.md`, `Modules/Launcher_CEF.md`, `Modules/UI_Bundles.md`
6. `Contracts/` (только по ссылкам из документов выше)
7. `Contracts/Codex_ResponseMode_Settings_Architecture.md`
8. `Plans/` (только при запуске нового scope, deferred work или design intake перед `todo-plan.md`)

## 1) Канонические документы (этот каталог)

### Workflow Overview
- `System/WorkflowSteps_Overview.md` — SSOT шагов workflow (1→6), артефактов, OUTDATED propagation.

### System
- `System/SystemArchitecture.md` — SSOT всей системы и глобальных инвариантов, включая Core-level provider-native request capture diagnostics.
- `System/Workflow_NewStep_Rollout_Guardrails.md` — SSOT protocol for adding or retrofitting workflow steps without split truth, startup asymmetry, continuity drift, or release-time regressions.

### Clusters
- `Clusters/Project_Manager.md` — SSOT подсистемы Project Manager.
- `Clusters/CoreOrchestrator.md` — SSOT подсистемы Core Orchestrator.

### Modules
- `Modules/Claude.md` — SSOT Claude provider module, including provider-home runtime isolation and SDK isolation-mode rules for CodeAI Hub-managed turns.
- `Modules/Codex.md` — SSOT Codex provider module, including reasoning summary settings and provider-home config policy.
- `Modules/Codex_ProviderInvocationFlags.md` — SSOT actual Codex App Server startup flags, JSON-RPC `initialize` / `thread/start` / `thread/resume` / `turn/start` payloads, model/reasoning resolution, provider-home env, and native request capture parity.
- `Modules/Gemini.md` — SSOT Gemini provider module.
- `Modules/Shared_RuntimeTranslation_Module.md` — SSOT shared runtime translation module.
- `Modules/Localization.md` — SSOT persistent UI localization module, including the four user-facing categories and the English-only internal-instructions boundary.
- `Plans/Archive/Localization_TranslationEngine_AnthropicHaiku_Architecture.md` — archived Anthropic Claude Haiku 4.5 translation engine architecture (closed by release `1.1.986`; canonical SSOT lives in `Modules/Claude.md`, `Modules/Shared_RuntimeTranslation_Module.md`, and `Modules/Localization.md`).
- `Modules/Launcher_CEF.md` — SSOT CEF Launcher module.
- `Modules/UI_Bundles.md` — SSOT UI bundles (Webview + Project Manager), включая Settings -> General card для provider native request capture commands.
- `Modules/Session_UI/README.md` — factual inventory of the five Session UI panels inside Project Manager, including truth-paths, update channels, outputs, side effects, and code ownership.

### Contracts (активные)
- `Contracts/DescriptionStep_SingleAgent.md` — канонический контракт шага `Description` (single-agent, file-first).
- `Contracts/VirtualSimulation_Step.md` — контракт шага `Virtual Simulation`.
- `Contracts/Workflow_CLI.md` — state machine шагов + watcher.
- `Contracts/ProjectManager_DescriptionEntry_CopyRefactor.md` — UI-контракт pre-submit/post-submit для Description.
- `Contracts/Dialogs_And_Continuity_Routing.md` — routing диалогов + continuity.
- `Contracts/SessionContinuity.md` — continuity handoff/rollover contract.
- `Contracts/WorkspaceRuntime.md` — multi-workspace + snapshot-first + lock contract.
- `Contracts/SessionUI_Behavior.md` — Session UI laws (happy path) + lock/unlock rules.
- `Contracts/SessionInputLock_SSOT_StateMachine.md` — SSOT/state machine для input lock/unlock.
- `Contracts/Codex_ResponseMode_Settings_Architecture.md` — response modes (`Strict` / `Hybrid` / `Debug/Raw`) + raw provider diagnostics contract для Codex.
- `Contracts/EffectiveModelIdentity_And_Settings_SSOT.md` — canonical effective model identity and next-turn settings SSOT.
- `Contracts/Gemini_ThoughtTranslation.md` — реализованный контракт перевода Gemini `Thought` событий в видимые tagged assistant messages.
- `Contracts/ProviderFailure_Recovery_And_ProviderSwitch.md` — deferred SSOT для provider failure classification, recovery и provider-neutral switch transfer.
- `Contracts/UserFacing_Text_Localization_Boundary.md` — SSOT text-ownership contract for `UI Labels`, `UI Helper Text`, `Messages for the User`, `Artifacts for the User`, and English-only `Internal Agent Instructions`.
- `Contracts/FacadeClassDiagram_DesignAndMaintenance.md` — process: фасады/границы/диаграммы (required reading).

### Checklists (periodic workflows)
- `Checklists/PeriodicAudit.md` — чек-лист периодического аудита кодовой базы (раз в 3-5 релизов): parallel audit passes для dead code + broken docs links, jscpd классификация top-20 клонов по 6 категориям (LEGIT-PROVIDER / LEGIT-BOUNDARY / LEGIT-SIMILAR-BUT-DIVERGING / EXTRACT-EASY / EXTRACT-COMPLEX / WITHIN-FILE-BUG), trzy-проход grep для локализационных ключей перед deletion, execution через отдельный cycle с planning-doc + todo-plan. Precedent: 1.2.10 Audit Cleanup.

### Contracts (compat / legacy filenames)
- `Contracts/DescriptionNode_ReviewSession.md` — legacy filename; redirect/compat note для узла `description`, без restart/reviewer semantics как активной возможности продукта.
- Остальные compat-redirect stubs из `Contracts/` (`Description_LegacyCleanup_Architecture`, `ProjectManager_VirtualSimulation_ColdStartRecovery`, `ProviderSessionHome_IsolationAndRecovery`, `ProviderSessionHome_SnapshotEngine_Design`, `StandaloneReviewer_Module`) удалены в рамках Phase 2 cleanup (Session025, 2026-04-09); исторические planning-доки, где они существуют, лежат в `Plans/Archive/`.

### Plans (pre-implementation / non-SSOT)
- `Plans/README.md` — правила жизненного цикла planning-доков.
- `Plans/ModelInvocationProfiles_And_TemplateSync_Architecture.md` — active planning-doc for provider-neutral model invocation profiles, Codex App Server GPT translation, text-only user-editable instruction templates, protected template sync, and Translation scenario native request capture.
- `Plans/Codex_AppServer_Capabilities_Analysis.md` — research/capabilities analysis по публичной документации OpenAI, сгенерированной схеме App Server и локальной интеграции CodeAI Hub; фиксирует product-level возможности `codex app-server` (thread lifecycle, approvals, plan/diff updates, MCP, auth, steer, review, multi-client coordination, динамические стартовые инструкции) и почему App Server является текущей интеграционной поверхностью Codex-провайдера.
- `Plans/Instruction_Stack_Control_Experiment_Results/Experiment_Results.md` — результат Claude/Codex instruction-stack эксперимента: доказанные ручки управления provider/system prompt, project instruction sources, workflow first user prompt, модельные Codex base prompts 5.3/5.4/5.5, архив prompt artifacts, текущий universal early-architecture Codex system prompt candidate и подтвержденный Codex documentation tool profile релиза `1.2.82`.
- `Plans/Instruction_Stack_Control_Experiment_Results/Codex_Workflow_Documentation_Tool_Profile.md` — technical protocol завершенного Codex documentation-tree tool-profile эксперимента: подтвержденные App Server startup flags/config overrides, expected/retest evidence и оставшийся `request_user_input` follow-up.
- `Plans/Claude_Agent_SDK_Capabilities_Analysis.md` — research/capabilities analysis по публичной документации Anthropic и локальной интеграции; фиксирует реальные возможности `@anthropic-ai/claude-agent-sdk`, включая `systemPrompt`, `settingSources`, hooks, MCP, сабагентов, custom tools и runtime session controls, чтобы оценивать дальнейшую эволюцию линии Claude без смешения с Codex/App Server.
- `Plans/CrossProvider_Common_Capabilities.md` — decision-oriented research-doc, который сводит пересечение возможностей Codex App Server и Claude Agent SDK в единый safe baseline для общего UI/ядра CodeAI Hub; первым кандидатом выделяет `WorkflowInstructionProfile`, а provider-specific возможности оставляет внутри адаптеров или advanced-настроек.
- `Plans/Archive/Provider_Instruction_Stack_Tuning_Tests_1.2.82.md` — archived umbrella planning-doc для Claude/Codex instruction-stack tuning tests; итоговые результаты перенесены в `Plans/Instruction_Stack_Control_Experiment_Results/Experiment_Results.md`, `Modules/Claude.md` и `Modules/Codex.md`.
- `Plans/Archive/Main_Merge_Release_Verification_1.2.83.md` — archived release-verification planning-doc: confirmed that merged `main` builds release `1.2.83`, including provider/core/UI/launcher tarballs and `codeai-hub-1.2.83.vsix`.
- `Plans/Archive/Settings_General_Footer_And_Codex_Models_1.2.84.md` — archived maintenance planning-doc для Settings General footer scroll boundary и добавления Codex моделей `gpt-5.2` / `gpt-5.3-codex-spark`; канонические итоги живут в `Modules/UI_Bundles.md`, `Modules/Codex.md`, shared Codex model registry и Core settings normalizer.
- `Plans/Archive/Settings_Footer_Codex_Models_Release_1.2.84.md` — archived release planning-doc: confirmed release `1.2.84` builds provider/core/UI/launcher tarballs and `codeai-hub-1.2.84.vsix` for Settings footer and Codex model catalog changes.
- `Plans/Archive/Codex_Progress_Updates_Instruction_Tuning_1.2.85.md` — archived experimental prompt-tuning planning-doc: Codex early-architecture system prompt now asks for ordinary user-visible assistant chat messages for progress updates, keeps `Codex_My_System_Prompt.md` in exact sync with runtime, moves README release history to `CHANGELOG.md`, and confirms release `1.2.85` artifacts plus `codeai-hub-1.2.85.vsix`.
- `Plans/Archive/Codex_Progress_Updates_Cadence_Tuning_1.2.86.md` — archived prompt-cadence tuning planning-doc: Codex early-architecture system prompt keeps ordinary user-visible assistant messages and adds 30-second / 3-5 work-cycle progress-update fallback; confirms release `1.2.86` artifacts plus `codeai-hub-1.2.86.vsix`.
- `Plans/Archive/Codex_Reasoning_Paragraph_Streaming_1.2.87.md` — archived Codex reasoning streaming planning-doc: app-server reasoning summaries now emit completed summary paragraphs before final item completion, use stable per-block message ids, feed per-block translation overlays, and confirm release `1.2.87` artifacts plus `codeai-hub-1.2.87.vsix`.
- `Plans/Archive/Codex_Reasoning_Translation_Timeout_1.2.88.md` — archived Codex reasoning translation timeout planning-doc: live reasoning overlay translation now uses a 15-second base timeout plus per-character allowance capped at 30 seconds, reducing fallback English paragraphs for Codex reasoning summaries; confirms release `1.2.88` artifacts plus `codeai-hub-1.2.88.vsix`.
- `Plans/Codex_Progress_Message_Regression_Diagnostics_1.2.91.md` — active diagnostic report for the rollback/retest line after releases `1.2.87` through `1.2.93`; records that system instructions are present in provider-native capture, `thread_log_created` and split file names are not the primary trigger, and release `1.2.93` confirmed Codex works without SDK transport logs.
- `Plans/Archive/Codex_Spark_Reasoning_Summary_Config_1.2.97.md` — archived hotfix planning-doc for Spark after release `1.2.96`: `gpt-5.3-codex-spark` keeps `turn/start.summary` omitted, provider-home `model_reasoning_summary = "auto" | "none"` is materialized for compatibility, non-Spark Codex models keep explicit per-turn summary payloads, and release `1.2.97` produced `codeai-hub-1.2.97.vsix`; user retest still showed no visible Spark reasoning summaries, now treated as provider-side limitation.
- `Plans/Archive/Codex_Progress_Update_NonTerminal_1.2.95.md` — archived prompt-guard planning-doc for the `1.2.94` retest failure where Codex `gpt-5.2` emitted a normal progress message and immediately completed the turn before creating the promised `Final_Description.md`; release `1.2.95` added the shared non-terminal progress-update rule, and follow-up release `1.2.96` fixed the Spark explicit reasoning-summary request compatibility issue.
- `Plans/Archive/Provider_SDK_Logs_Removal_Refactor_1.2.94.md` — archived refactor planning-doc for removing provider-owned SDK/raw file-backed logs under `~/.codeai-hub/logs/{claude,codex,gemini}` from runtime construction, call sites, tests, and SSOT documentation while preserving session-local history, provider-home artifacts, native request capture, and Core/reporter diagnostics.
- `Plans/Archive/Codex_Instruction_Stack_StepByStep_Flag_Tests_1.2.75.md` — archived Codex step-by-step instruction/config flag planning-doc: full-capture baseline, `project_doc_max_bytes = 0`, compact/baseInstructions test, rollback to full base prompt and evidence-gated comparison.
- `Plans/Archive/Codex_GPT55_Model_Addition_1.2.76.md` — archived planning-doc для добавления `gpt-5.5` в Codex settings/model registry и Settings -> General native request capture selector; итоговая модельная инвентаризация зафиксирована в experiment result bundle.
- `Plans/Archive/Provider_AutoUpdate_And_Claude_Preflight_1.2.78.md` — archived technical-debt planning-doc: Core startup auto-update по Project Manager settings, Claude auth preflight через installed executable path и `provider_not_ready` diagnostic reason.
- `Plans/Archive/Claude_Workflow_Tool_Profile_Allowlist_1.2.80.md` — archived test planning-doc для Claude Agent SDK `tools` allowlist; retest подтвердил provider-visible `Edit`, `Read`, `Write`.
- `Plans/Archive/Codex_Workflow_MultiAgent_Tool_Profile_Flag_1.2.81.md` — archived Codex App Server `--disable multi_agent` tool-profile experiment; retest подтвердил удаление subagent tool family.
- `Plans/Archive/Claude_Instruction_Stack_Flag_Evidence.md` — archived evidence-документ завершенного Claude instruction-stack этапа релиза `1.2.70`; фиксирует baseline C0 без explicit `systemPrompt`, C1 `claude_code` preset с большим `body.system`, C2 custom-only neutral `systemPrompt`, неизменный `body.tools` hash и итоговую передачу финального контракта в `Modules/Claude.md`.
- `Plans/Archive/Native_Request_Capture_Codex_Turn_Context_Hotfix_1.2.67.md` — archived hotfix planning-doc релиза `1.2.67`; фиксирует Codex native capture regression из `1.2.66`, где WebSocket capture показывал ранний `response.create` frame (`generate:false`, `input:[]`) без workflow first user prompt. Итог: provider diagnostic context records в `.jsonl` / `.md`, Codex app-server `thread/start` / `turn/start` request/response payloads в отдельной секции `Provider Diagnostic Context`, сохранение provider-native WebSocket instructions/tools отдельно от app-server turn payload.
- `Plans/Archive/Provider_Native_Request_Capture_Architecture.md` — archived planning-doc закрытого diagnostic/release scope релиза `1.2.62`; фиксирует Settings -> General buttons `Capture Claude Native Request` / `Capture Codex Native Request`, Core `provider-network-capture` TLS MITM capture-and-abort mode, `.jsonl` / `.md` artifacts under `~/.codeai-hub/logs/native-request-capture/`, Claude Agent SDK capture path and isolated temporary Codex App Server capture path. Канонический итог живёт в `System/SystemArchitecture.md`, `Modules/Claude.md`, `Modules/Codex.md` и `Modules/UI_Bundles.md`.
- `Plans/Archive/Native_Request_Capture_Workflow_Scenarios_1.2.66.md` — archived planning-doc релиза `1.2.66`; фиксирует workflow scenario selector в Settings -> General для provider native request capture, PM-built first-turn prompts для `Description` / `Virtual Simulation` / `Diagram Modules`, Core/provider prompt threading и Codex multi-frame WebSocket capture с выбором full-turn frame. Канонический итог живёт в `System/SystemArchitecture.md`, `Modules/UI_Bundles.md`, `Modules/Claude.md` и `Modules/Codex.md`.
- `Plans/Archive/Native_Request_Capture_Runtime_Hotfix_1.2.63.md` — archived hotfix planning-doc релиза `1.2.63`; фиксирует live-test regression из `1.2.62`, где Core вызывал `adapter.captureNativeRequest` без class receiver и provider adapters теряли `this.nativeRequestCaptureService`. Итог: bound adapter call, provider runtime error записи в JSONL/Markdown, cleanup pending proxy timeout и regression test для class-style adapter. Канонический итог остаётся в `System/SystemArchitecture.md` и module docs по provider-native capture.
- `Plans/Archive/Native_Request_Capture_WebSocket_And_Trace_Hotfix_1.2.64.md` — archived hotfix planning-doc релиза `1.2.64`; фиксирует retest 1.2.63, где Codex capture видел только WebSocket upgrade `GET /backend-api/codex/responses`, а Claude ignored requests были слишком sparse. Итог: local WebSocket `101` accept + first client frame capture, richer ignored request JSONL/Markdown diagnostics, targeted tests и VSIX `codeai-hub-1.2.64.vsix`. Канонический итог остаётся в `System/SystemArchitecture.md`, `Modules/Claude.md` и `Modules/Codex.md`.
- `Plans/Archive/Native_Request_Capture_AppPath_Tuning_1.2.65.md` — archived tuning/release planning-doc релиза `1.2.65`; фиксирует model selector в Settings -> General, протяжку `modelId` через UI/PM/Core, reuse Core-applied turn config для diagnostic capture, Claude thinking/effort parity, Codex effort/summary parity и Markdown/JSONL readability для selected/applied config plus Codex `instructions` / `input`. Канонический итог живёт в `System/SystemArchitecture.md`, `Modules/Claude.md` и `Modules/Codex.md`.
- `Plans/Archive/Claude_SessionListMarker_Formatting_Architecture.md` — archived planning-doc закрытого bugfix/release scope релиза `1.2.26`; фиксирует Claude marker-safe live flush contract, thinking merge repair guard в Project Manager и возврат Session list markers к outside rendering.
- `Plans/Archive/Codex_Commentary_And_ThinkingBlockFormatting_Architecture.md` — archived planning-doc закрытого bugfix/release scope релиза `1.2.27`; фиксирует возврат Codex `phase: "commentary"` в app-server dialog trail как non-terminal assistant message, block-aware merge contract для standalone bold heading sections в thinking cards и follow-up CSS tightening для heading/body spacing.
- `Plans/Archive/DialogPanel_LocalizedLastBubble_Autoscroll_Architecture.md` — archived planning-doc закрытого UI bugfix scope от `2026-04-20`; фиксирует bottom-lock autoscroll contract для Session dialog: late translation overlay patch последней visible bubble должен менять scroll anchor по display payload (`localizedContent ?? content`) и повторно доскролливать контейнер к низу, если пользователь уже был pinned-to-bottom.
- `Plans/Archive/DialogPanel_LocalizedLastBubble_Autoscroll_Release_1.2.28.md` — archived planning-doc закрытого release scope релиза `1.2.28`; фиксирует выпуск VSIX/tarball пакета для autoscroll fix, где late localized growth последней bubble повторно триггерит bottom-lock автопрокрутку через display-payload scroll anchor.
- `Plans/Archive/SessionDialog_BubbleVisualTone_Architecture.md` — archived planning-doc закрытого UI tuning scope от `2026-04-20`; фиксирует общий visual contract Session dialog bubble cards: shared `1px` stroke для всех message cards и muted thinking surface across Claude/Codex/Gemini через alpha-softened bubble chrome и typography.
- `Plans/Archive/SessionDialog_BubbleVisualTone_Release_1.2.29.md` — archived planning-doc закрытого release scope релиза `1.2.29`; фиксирует выпуск VSIX/tarball пакета для Session dialog visual tone tuning с новым shared `1px` stroke и приглушёнными thinking cards.
- `Plans/Archive/SessionDialog_ThinkingCompositeAndShadow_Architecture.md` — archived planning-doc закрытого UI bugfix scope от `2026-04-20`; фиксирует split между legacy compact `role="thinking"` strip и реальной assistant-tagged provider-facing `Thinking` bubble, возврат shadow на user-facing bubble path и backdrop-tuned chrome для общего shared contract across Claude/Codex/Gemini.
- `Plans/Archive/SessionDialog_ThinkingCompositeAndShadow_Release_1.2.33.md` — archived planning-doc закрытого release scope релиза `1.2.33`; фиксирует выпуск VSIX/tarball пакета для corrected provider-facing `Thinking` bubble contract, где user-facing `Claude · Thinking` / `Codex · Thinking` / `Gemini · Thinking` снова рендерятся как full cards with restored shadow and backdrop-tuned muted chrome.
- `Plans/Archive/SessionDialog_ThinkingTextOpacity_Release_1.2.35.md` — archived planning-doc закрытого release scope релиза `1.2.35`; фиксирует retune основного thinking body text до `rgba(173, 178, 186, 0.7)` при сохранении остального visual contract релиза `1.2.34`, включая muted fill/stroke/shadow и более приглушённый timestamp.
- `Plans/Archive/DiagramModules_TwoColumnModuleTable_Cleanup_Architecture.md` — archived planning-doc закрытого cleanup scope релиза `1.2.37`; завершает refactor `c488df065` (удаление `ModuleKind`) в двух parser'ах артефакта `product-parts/<part-id>.md`: browser staged-part parser и Core `development-tree-snapshot.ts` теперь принимают канонический 2-колоночный module table (`| \`module-id\` | Responsibility |`), а `development-tree-snapshot` ограничивает `## Standalone Modules` body следующим `##`-заголовком, чтобы строки `## Simple Relations` перестали утекать как фантомные standalone modules. Invariant вынесен в SystemArchitecture §6.4.
- `Plans/Archive/SessionInputLock_RuntimeMaterialization_Architecture.md` — archived planning-doc закрытого scope релиза `1.2.39`; фиксирует Core-side fix для `BUG-2026-04-21-01` (reopened workflow dialog залипает в `Agents is working` + Stop не работает на нём после cold-start): `RemoteBridgeDialogCommandRouter.handleDialogList` через новый `materializeContinuityEntries` helper создаёт stub runtime session + paper-binding + `WorkspaceRuntimeFacade.notifySessionCreated` для каждой `ContinuityIndexEntry` с полной связкой `latestSessionId + providerId + providerSessionId`, если она ещё не в SessionManager. `SessionManager.registerSessionWithId` и `SessionProviderBindingService.registerRestoredBinding` — новые public seam-ы. Provider `thread/resume` остаётся ленивым. PM-side не трогается. Invariant вынесен в `SessionInputLock_SSOT_StateMachine.md` §3.3, `SessionUI_Behavior.md` §4.4, `CoreOrchestrator.md` §3, `SystemArchitecture.md` §3 Invariant 1.
- `Plans/Archive/DiagramModules_DevTreeParser_And_AutoFitZoom_Architecture.md` — archived planning-doc закрытого scope релиза `1.2.40`; фиксирует Core-side fix для `BUG-2026-04-21-02` (Development Tree sidebar показывал фантомные standalone modules и нестабильно перещёлкивал структуру на expand/collapse кластеров в `diagram_modules` артефактах) и PM-side fix для Artifacts diagram auto-fit zoom (композиция product parts уходила за правую границу panel при уменьшении ширины окна). Первый фикс: `packages/core/src/remote-bridge/handlers/development-tree-snapshot.ts` перевёл все `/g`-regex на `.matchAll()` / factory-функции / `.search()` на non-global regex (вместо прямого `.exec()` на module-level singleton, чей `lastIndex` сохранялся между вызовами в long-lived Core процессе и давал hit/null alternation), а `MODULE_ROW_RE` ужесточён до строго 2-column с `[^|\n]+` во второй колонке и якорем `\|[ \t]*$` — 4-колоночные Simple Relations rows физически не матчатся. Второй фикс: `src/client/project-manager/components/diagram-editor/diagram-editor-facade.tsx` ввёл auto-fit через `ResizeObserver` + natural `scrollWidth`, effective scale = `autoFitScale × userZoom`, manual Cmd/Ctrl+scroll остаётся как overlay, Cmd/Ctrl+0 сбрасывает только user overlay. Invariant вынесен в `SystemArchitecture.md` §6.4 (regex lastIndex safety + auto-fit zoom contract).
- `Plans/Archive/Usage_Limits_AccountScoped_Warmup_Architecture.md` — archived planning-doc закрытого hotfix scope релиза `1.2.44`; фиксирует `BUG-2026-04-21-06` (usage-limits виджет у Claude и Codex показывал пустое или фейковое `0%` в окне между Core restart и первым turn'ом). Phase 1 rescope был no-op — `buildProviderUsageLimitScopeKey` уже возвращает `{providerId}:global` account-wide (с `070139b9e`). Phase 2 делает единственный dispatch: новый `UsageLimitsWarmupTracker: Set<providerId>` в `SessionRequestHandler`, binding_ready triggers для уже warmed провайдера → replay-only; другие lifecycle triggers (`turn_completed`, `reconnect`, `manual`, `provider_session_rebound`, `dialog_opened`, `session_opened`) dedup не затрагивает. `handleRefreshUsageLimits` делегирован в helper `handleRefreshUsageLimitsFlow` (извлечён в отдельный файл для удержания `session-request-handler.ts` под 500-строковым лимитом). Phase 3 UI fallback был skipped — `usage-limits-stream.readBucket` уже возвращает null на `percentUsed === null`, `hasUsageLimits` скрывает row целиком, widget честен; legitimate 0% в начале 5-часового окна остаётся визуально `Session 0% (Resets …)`. Invariant вынесен в `SystemArchitecture.md` §3 Invariant 1 (single-probe usage_limits warmup policy).
- `Plans/Archive/UsageLimits_PreTurn_DialogOpen_Refresh_Architecture.md` — archived planning-doc закрытого functional-addition scope релиза `1.2.45`; фиксирует truthful pre-turn usage-limits UX для reopened Claude/Codex dialogs без eager provider resume. PM теперь хранит provider-scoped usage telemetry (`{providerId}:global`) независимо от существования source snapshot и seed-ит reopened runtime/dialog snapshots из этого кэша, поэтому previously seen account-wide limits и reset annotations могут отображаться сразу при открытии шага. Дополнительно введён explicit lifecycle trigger `dialog_opened`: Core сначала replay-ит cached payload, затем делает cheap provider-scoped refresh без hydration dialog/session (`account/rateLimits/read` для Codex, headers probe `max_tokens: 1` для Claude). Empty warmup probe больше не permanently warm-ит провайдера; `SessionIdBar` показывает явный pending/unavailable state вместо silent empty rails и сохраняет формат `Session N% (Resets ...) / Weekly M% (Resets ...)` для 5-часового и недельного окон сразу после seeded/open-time refresh. Канонический итог вынесен в `SystemArchitecture.md` §3 Invariant 1, `SessionUI_Behavior.md` и `Dialogs_And_Continuity_Routing.md`.
- `Plans/Archive/CEF_MacOS_BootstrapHardening_Architecture.md` — archived planning-doc закрытого launcher-hardening scope релиза `1.2.46`; фиксирует переход macOS launcher bootstrap на CEF-compatible custom `NSApplication <CefAppProtocol>` + delegate-driven lifecycle, чтобы path `quit/close/reopen` следовал официальному CEF sample вместо упрощённого `NSApplication sharedApplication` bootstrap и не провоцировал intermittent `NSInvalidArgumentException` / `unrecognized selector` crash при закрытии Project Manager. Канонический итог вынесен в `Modules/Launcher_CEF.md` и `SystemArchitecture.md` (Invariant 32).
- `Plans/Archive/CEF_MacOS_CanClose_ShortCircuit_Architecture.md` — archived planning-doc закрытого primary-fix scope релиза `1.2.52` для `BUG-2026-04-22-01`. После того как обе exception-pipeline попытки (1.2.50 NSSetUncaughtExceptionHandler, 1.2.51 reportException: swizzle) в user retest подтвердились как недостаточные, подход был пересмотрен: не ловить exception, а не запускать buggy Chromium teardown callback вообще. `LauncherWindowDelegate::CanClose` в `packages/cef-launcher/src/launcher_app.cc` на macOS (`#if defined(__APPLE__)`) вместо `browser->GetHost()->TryCloseBrowser()` вызывает новый cross-platform helper `codeai::launcher::RequestNativeApplicationTermination()` и возвращает `false`. Helper declared в `launcher_handler.h` (namespace `codeai::launcher`), implemented в `platform/mac/launcher_handler_mac.mm` через `[NSApp terminate:nil]`. Красная close кнопка теперь идёт по тому же pathway что Cmd+Q / Dock Quit: `-[NSApplication terminate:]` → `-[NSApplication stop:]` → clean AppKit unwind → `main()` returns → `CefShutdown()`. Buggy Chromium callback не запускается, exception физически не кидается, `+[NSApplication _crashOnException:]` не вызывается, crash dialog не появляется — это настоящий fix, а не mitigation. Windows/Linux branch оставлен без изменений. 1.2.51 `reportException:` swizzle в `app_main_mac.mm` retained как belts-and-suspenders safety net на случай если в CEF views framework остался ещё один path с тем же exception signature. Paste/Cmd+V/SuperWhisper/Cmd+C-X-A/Cmd+Q/Dock Quit/dock reopen поведение 1.2.49-1.2.51 полностью сохраняется. Root cause (Chromium 141 ↔ macOS 26.3.1 incompat) всё ещё deferred to CEF/Chromium upgrade, но urgency снижена — observable crash устранён. SSOT обновлён: `SystemArchitecture.md` Invariant 32 переписан под 1.2.52 short-circuit + retained 1.2.51 swizzle; `Launcher_CEF.md` получил subsection "Shutdown-crash primary fix (1.2.52 — CanClose short-circuit)" перед superseded-but-retained 1.2.51 subsection; `BugRegistry.md` `BUG-2026-04-22-01` → Status FIXED.
- `Plans/Archive/ProjectManager_Settings_And_RuntimeOwnership_Architecture.md` — archived planning-doc закрытого release scope релиза `1.2.53`; фиксирует ownership migration для Settings и runtime bootstrap boundaries. Исторический итог `1.2.53`: ownership был переведён в `Core` + `Project Manager`, а первый PM-owned Settings surface временно открывался в detached окне `?mode=detached-settings`. Текущий канонический итог после follow-up релиза `1.2.54` другой: `Project Manager` остаётся единственным живым Settings UI, но уже через in-shell right-panel takeover; `VS Code extension` по-прежнему де-скоуплен до distribution/install/bootstrap-components shell без runtime startup authority и с compat-only settings notice. Актуальный SSOT искать в `SystemArchitecture.md`, `Clusters/Project_Manager.md` и `Modules/UI_Bundles.md`.
- `Plans/Archive/Settings_CanonicalPath_Fix_1.2.61.md` — archived planning-doc закрытого fix/release scope релиза `1.2.61`; фиксирует hard cutover unified runtime settings snapshot на `~/.codeai-hub/settings/settings.json`, удаление live fallback/write-path через `claude.json` и Core-side materialization свежего canonical файла при его отсутствии. Канонический итог вынесен в `SystemArchitecture.md` и `Contracts/EffectiveModelIdentity_And_Settings_SSOT.md`.
- `Plans/Archive/PM_DetachedDiagram_Popup_Lifecycle_Architecture.md` — archived planning-doc закрытого popup-lifecycle scope релиза `1.2.56`; фиксирует split между main PM window и detached `Diagram Modules` popup на macOS launcher boundary: auxiliary popup больше не маршрутизируется в whole-app terminate path, не reuse-ит main-window autosave geometry и получает popup-sized open hint из PM detach action. Канонический итог вынесен в `BugRegistry.md`, `Modules/Launcher_CEF.md` и `Clusters/Project_Manager.md`.
- `Plans/Archive/PM_StatusBar_OpenSettings_Cleanup_Architecture.md` — archived planning-doc закрытого UI polish scope релиза `1.2.57`; фиксирует очистку PM footer (`pm-status-bar`): левая часть с плашкой `CONTEXT` и именем workspace удалена как дубликат workspace selector'а в левом sidebar, prop `workspaceName` убран из `StatusBar`, неиспользуемые локализационные ключи (`pm.status_bar.context_label`, `pm.status_bar.no_workspace_label`) вычищены из approved dictionary. Кнопка `Open Settings` выведена в primary action через выделенный CSS-класс `pm-status-open-settings` с accent-цветом и тремя visible фазами (`default` / `hover` / `active`) + `focus-visible` outline, typography выровнена с `WORKFLOW TREE MVP` (uppercase, letter-spacing, 12px). Канонический итог вынесен в `Clusters/Project_Manager.md` и `Modules/UI_Bundles.md`.
- `Plans/Archive/PM_Settings_InShell_Stabilization_Architecture.md` — archived planning-doc закрытого stabilization scope релиза `1.2.54`; корректирует post-release regressions `1.2.53` и переводит PM Settings из detached popup-path в in-shell right-panel takeover. Канонический итог: `Project Manager` оставляет за собой единственный live Settings surface, но теперь внутри основного окна; shared `Core Controls` и `Restart Core` восстановлены через PM + launcher restart bridge (`codeai://core-restart` / `RestartCoreProcess()`), а overlay `Synchronizing localization` ограничен реальным `settings:localization-sync-status`. SSOT обновлён в `SystemArchitecture.md`, `Clusters/Project_Manager.md`, `Modules/UI_Bundles.md`, `Modules/Launcher_CEF.md`; баги `BUG-2026-04-22-05/06/07` закрыты в `BugRegistry.md`; релизный rebuild завершён в `1.2.54`.
- `Plans/Archive/PM_TranslationEngineSelector_CEF_Crash_Architecture.md` — archived planning-doc закрытого crash-fix scope релиза `1.2.55`; фиксирует post-release regression `BUG-2026-04-22-08`, где standalone Project Manager на macOS 26.x падал при открытии `UI Translation Engine` из-за Chromium/AppKit native popup path для HTML `<select>`. Канонический итог: shared `TranslationEngineSelector` больше не использует native `<select>` и переведён на DOM-owned button/listbox surface, поэтому и `UI Translation Engine`, и `Reasoning Translation Engine` больше не заходят в AppKit-native popup branch. Launcher bridge не расширялся; SSOT обновлён в `Modules/UI_Bundles.md` и `Modules/Launcher_CEF.md`; релизный rebuild завершён в `1.2.55`.
- `Plans/Archive/CEF_MacOS_ReportException_Swizzle_Architecture.md` — archived planning-doc закрытого follow-up mitigation scope релиза `1.2.51`; после того как user retest 1.2.50 подтвердил что `NSSetUncaughtExceptionHandler` подход не сработал (AppKit переустанавливает свой handler на `-[NSApplication finishLaunching]` поверх нашего; `+[NSApplication _crashOnException:]` обходит стандартную uncaught-handler chain на macOS 26), переключились на Objective-C method swizzle на `-[NSApplication reportException:]`. Category `NSApplication (CodeAIHubReportExceptionSuppression)` в `app_main_mac.mm` с `+load` → `method_exchangeImplementations` — Objective-C runtime делает swap во время dyld image load (до `main()`, до AppKit/CEF init), AppKit не может undo swap. После exchange вызов AppKit'ом `-reportException:` dispatch'ится в `codeai_reportException:`, который ловит `NSInvalidArgumentException` с reason содержащим `unrecognized selector sent to instance` и `NSApplication`, логирует в stderr, возвращается без вызова `+[NSApplication _crashOnException:]`. Non-matching exceptions forward'ятся в original IMP через swizzle trampoline. Dead 1.2.50 `NSSetUncaughtExceptionHandler` код удалён в том же commit'е. Mitigation не меняет NSApplication class, Info.plist, window-close flow, NSApplicationDelegate — clipboard/quit behaviour 1.2.49/1.2.50 полностью сохраняется. SSOT обновлён: `SystemArchitecture.md` Invariant 32 переписан под swizzle, `Launcher_CEF.md` subsection переписан под 1.2.51, `BugRegistry.md` `BUG-2026-04-22-01` остаётся MITIGATED но current-resolution block теперь про swizzle. Root cause (Chromium 141 ↔ macOS 26 incompat) всё ещё deferred to CEF/Chromium upgrade.
- `Plans/Archive/CEF_MacOS_Shutdown_Crash_Mitigation_Architecture.md` — archived planning-doc закрытого mitigation scope релиза `1.2.50`; добавляет `InstallCodeAIHubUncaughtExceptionHandler()` в `packages/cef-launcher/src/platform/mac/app_main_mac.mm` (вызов из `main()` после `CefScopedLibraryLoader::LoadInMain()` до `CefExecuteProcess`) как точечный workaround против `NSApplication unrecognized selector` crash (BUG-2026-04-22-01). Handler ловит `NSInvalidArgumentException` где reason содержит `unrecognized selector sent to instance` и `NSApplication`, логирует в stderr и swallow'ит; остальные exceptions forward'ятся в previous handler. User retest 1.2.49 уточнил trigger: crash детерминирован только на красной NSWindow close кнопке (`LauncherWindowDelegate::CanClose` → Chromium async browser-teardown), НЕ воспроизводится при Cmd+Q/Dock Quit (`-[NSApplication stop:]` path). Root cause — Chromium 141 отправляет AppKit-private selector, который не существует на macOS 26.3.1; proper fix требует CEF/Chromium upgrade и остаётся deferred. Mitigation не меняет NSApplication class, Info.plist, window-close flow, NSApplicationDelegate и clipboard/quit поведение 1.2.49 — Cmd+V/SuperWhisper/Cmd+C-X-A/Cmd+Q/Dock Quit/red close button/dock reopen работают как раньше. SSOT обновлён: `SystemArchitecture.md` Invariant 32 расширен mitigation note; `Launcher_CEF.md` получил subsection "Shutdown-crash mitigation (1.2.50)"; `BugRegistry.md` `BUG-2026-04-22-01` переведён из DEFERRED в MITIGATED с explicit note что proper fix deferred to CEF upgrade.
- `Plans/Archive/CEF_MacOS_Bootstrap_Rollback_Architecture.md` — archived planning-doc закрытого rollback scope релиза `1.2.49`; полностью отзывает CEF macOS bootstrap refactor из `1.2.46` + `1.2.48` (коммиты `de7c5ad37`, `b6b0cf3d1`, `a97c5e9c5`, `a6dd758b2`) после подтверждения user retest что `1.2.48` narrow fix (удаление Edit menu + стандартный `applicationShouldTerminate:`) не попал в реальный paste-breaker — root cause сидит внутри самого `CodeAIHubApplication : NSApplication <CefAppProtocol>` shell'а. Launcher возвращается к plain `[NSApplication sharedApplication]` bootstrap из `1.2.45` baseline (`app_main_mac.mm` checkout of `70ac9a6ac`, `codeai_hub_application_mac.{h,mm}` удалены, `CMakeLists.txt` очищен). Редкий `NSApplication unrecognized selector` crash-on-quit повторно открыт как DEFERRED known issue (`BUG-2026-04-22-01`) — acceptable trade-off против сломанного core PM workflow (Cmd+V + SuperWhisper). SSOT обновлён: `SystemArchitecture.md` Invariant 32 переписан как rollback note, Invariant 33 (1.2.48) удалён; `Launcher_CEF.md` macOS Bootstrap Lifecycle Boundary редуцирована до plain-NSApp contract; `BugRegistry.md` переоткрыл `BUG-2026-04-22-01` как DEFERRED, закрыл `BUG-2026-04-22-04` как FIXED (via rollback in 1.2.49).
- `Plans/Archive/CEF_MacOS_Input_And_Quit_Regression_Architecture.md` — archived planning-doc закрытого regression-fix scope релиза `1.2.48`; follow-up к `1.2.46`. Фиксирует `BUG-2026-04-22-04` (после перевода NSApp на `CodeAIHubApplication : NSApplication <CefAppProtocol>` CEF перестал runtime-swizzle-ить `-[NSApplication sendEvent:]`, и наш `[super sendEvent:]` прогоняет NSKeyDown через `NSMenu performKeyEquivalent:`; Cut/Copy/Paste/SelectAll menu items с `target:nil` перехватывали Cmd+X/C/V/A и не давали Chromium наблюдать key event → paste/SuperWhisper сломались; override `-[NSApplication terminate:]` с `CloseAllBrowsers(false)` зависал Dock Quit и Cmd+Q когда любой `TryCloseBrowser()` возвращал false). Fix: убран override `terminate:` и метод `tryToTerminateApplication:`; quit идёт стандартным AppKit маршрутом `terminate:` → `applicationShouldTerminate:`, delegate force-close-ит browsers через `CloseAllBrowsers(true)` и возвращает `NSTerminateCancel`; `OnBeforeClose` драйвит `CefQuitMessageLoop()`. Edit menu полностью удалён — Chromium сам обрабатывает clipboard shortcuts на render-side. Канонический итог вынесен в `Modules/Launcher_CEF.md` и `SystemArchitecture.md` §3 Invariant 33 (включая permanent CEF acceptance matrix перед release).
- `Plans/Archive/Codex_PATH_And_PostRebind_UsageLimits_Architecture.md` — archived planning-doc закрытого hotfix scope релиза `1.2.43`; фиксирует `BUG-2026-04-21-05` (два симптома после 1.2.42 user acceptance). Первый: Codex `Provider codexCli unavailable` после штатного close all sessions — inherited `process.env.PATH` у VS Code extension host не содержит `~/.npm-global/bin` на macOS GUI, каждый recovery re-spawn падал `spawn codex ENOENT`. Фикс: `buildAugmentedPath()` в `CodexAppServerProcess.startInternal` приписывает к PATH curated-список user-level install directories (`~/.npm-global/bin`, `/opt/homebrew/bin`, `/usr/local/bin`, `/usr/bin` на POSIX; `%APPDATA%\npm` на Windows). Второй: `usage_limits` виджет пустой у Claude/Codex после Core restart — PM emit'ит `binding_ready` refresh ровно один раз, первый refresh гонится против paper-binding hydration. Фикс: новый helper `triggerPostRebindUsageLimitsRefresh` вызывается из `SessionRequestHandlerMessageDispatch.retryAfterStaleBinding` после успеха rebind'а и прогоняет `adapter.refreshUsageLimits` через тот же `normalizeUsageLimitsStreamEvent` broadcast path. Invariant вынесен в `SystemArchitecture.md` §3 Invariant 1 (post-rebind usage_limits refresh required + Codex PATH augmentation note).
- `Plans/Archive/Provider_StaleBinding_AutoRecovery_Architecture.md` — archived planning-doc закрытого scope релиза `1.2.42`; фиксирует `BUG-2026-04-21-04` (первое сообщение в reopened Claude/Codex диалоге после Core restart молча теряется: 1.2.39 materializer корректно журналирует paper-binding с `providerSessionStatus: "ready"`, но dispatch считает ready за "provider hydrated" и слёт в `adapter.sendMessage` без resume; Claude SDK Manager и Codex app-server не знают `providerSessionId` → generic Error → classifier помечает retryable без wired retry → silent drop). Fix копирует Gemini-1.2.8 precedent: новые `ClaudeSessionStaleBindingError` и `CodexSessionStaleBindingError` с общим shape (`code`, `providerSessionId`); в Claude-модуле `ClaudeSDKManager.sendMessage` бросает typed error вместо generic; в Codex facade `handshakedThreadIds` Set заполняется в `createSession`/`resumeSession`, проверяется в `sendMessage` — отсутствие membership бросает typed error до `turn/start`; Core dispatch detector обобщён на `ReadonlySet<string>` кодов (GEMINI_/CLAUDE_/CODEX_), one-shot `invalidateProviderBinding + ensureSessionReadyForSend + resend` retry теперь работает для всех трёх провайдеров. Invariant вынесен в `SystemArchitecture.md` §3 Invariant 1 (`ready` paper-binding != "provider hydrated", adapter обязан throw'ить typed stale-binding error, generic Error недопустим).
- `Plans/Archive/DiagramModules_AutoFitZoom_NaturalWidthHotfix_Architecture.md` — archived planning-doc закрытого hotfix scope релиза `1.2.41`; фиксирует `BUG-2026-04-21-03` (после `1.2.40` composition на Artifacts panel уходила за правую границу panel даже при Cmd+Ctrl+0 и Cmd+scroll → 25%, потому что `width: max-content + minWidth: 100%` на inner div `DiagramEditorFacade` позволял prose и `1fr` column tracks раскатываться в unwrappable single lines, natural width становилась тысячами пикселей, auto-fit сваливался в floor `0.25`). Fix: убрать intrinsic-sizing keyword'ы с composition-container'а и вернуться к естественному grid sizing — `scrollWidth` на нормально-сайзнутом grid уже возвращает `max(clientWidth, rightmost-child.right)` и auto-fit measurement продолжает работать. Source-level regression assertion инвертирован на `max-content === false`. Invariant в `SystemArchitecture.md` §6.4 переписан без упоминания `max-content` / `min-width: 100%`.
- `Plans/Archive/LegacyCodexModule_Removal_Architecture.md` — archived planning-doc закрытого cleanup scope релиза `1.2.38`; физически удаляет legacy SDK-based `packages/Codex_Module/` (orphaned с релиза `1.2.22`) вместе с transitive `@openai/codex-sdk@0.53.0`, вычищает `knip.json` / `.vscodeignore` / `scripts/build-release.sh` / `package-lock.json` и переключает canonical SSOT документы (Modules/Codex.md, SystemArchitecture.md §4, Formal_Module_Cluster_Facade_Architecture.md mermaid, ProviderFailure_Recovery_And_ProviderSwitch.md, EffectiveModelIdentity_And_Settings_SSOT.md) с legacy fallback на `Codex_AppServer_Module` как single active implementation при полностью стабильном external installer contract (`codexCli`, `~/.codeai-hub/providers/codex`, artifact name `codex-module-<version>.tar.bz2`).
- `Plans/Archive/SessionDialog_ThinkingUnifiedChrome_Release_1.2.34.md` — archived planning-doc закрытого release scope релиза `1.2.34`; фиксирует унификацию chrome обоих внутренних thinking paths (`role="thinking"` и `assistant + tag="thinking"`) на общий muted fill/stroke и общий softened `50%` shadow перед выпуском VSIX/tarball пакета.
- `Plans/Archive/SessionDialog_ThinkingChromeExactColor_Architecture.md` — archived planning-doc закрытого UI retune scope от `2026-04-20`; фиксирует переход muted thinking chrome на точные design values `#2C3230 @ 45%` fill и `#47474A @ 45%` stroke при сохранённом provider-accent header contract.
- `Plans/Archive/SessionDialog_ThinkingChromeExactColor_Release_1.2.32.md` — archived planning-doc закрытого release scope релиза `1.2.32`; фиксирует выпуск VSIX/tarball пакета для corrective exact-color retune, где muted `Thinking` bubble chrome использует точные design values `#2C3230 @ 45%` fill и `#47474A @ 45%` stroke.
- `Plans/Archive/SessionDialog_ThinkingProviderAccent_Architecture.md` — archived planning-doc закрытого UI retune scope от `2026-04-20`; фиксирует corrective visual contract для assistant-tagged `Thinking` headers: provider hue сохраняется в заголовке на muted alpha, а fill/border thinking-card поднимаются до более читаемых `0.45`.
- `Plans/Archive/SessionDialog_ThinkingProviderAccent_Release_1.2.31.md` — archived planning-doc закрытого release scope релиза `1.2.31`; фиксирует выпуск VSIX/tarball пакета для corrective thinking-provider-accent retune, где `Codex · Thinking` / `Claude · Thinking` / `Gemini · Thinking` сохраняют provider hue в приглушённом заголовке и получают чуть более читаемый muted chrome.
- `Plans/Archive/SessionDialog_TaggedThinkingVisualTone_Architecture.md` — archived planning-doc закрытого bugfix scope от `2026-04-20`; фиксирует corrective visual contract для user-facing `Thinking` card на assistant-tagged path (`role="assistant" + tag="thinking"`), включая `Codex · Thinking`.
- `Plans/Archive/SessionDialog_TaggedThinkingVisualTone_Release_1.2.30.md` — archived planning-doc закрытого release scope релиза `1.2.30`; фиксирует выпуск VSIX/tarball пакета для corrective tagged-thinking visual fix, где реальный `Codex · Thinking` / provider-facing `Thinking` path получает muted alpha contract.
- `Plans/Archive/README_CurrentInstallationPath_Architecture.md` — archived planning-doc закрытого doc-only scope от `2026-04-20`; фиксирует добавление в `README.md` current installation path для технически подготовленных пользователей: early-access status, prerequisites, source-build steps, resulting artefacts и VSIX install flow.
- `Plans/Archive/Codex_FinalSummary_Reasoning_Rendering_Architecture.md` — archived planning-doc закрытого bugfix/release scope релиза `1.2.25`; фиксирует перенос user-facing Codex reasoning emission с live readable chunking на completed `item.summary[]` block emission и follow-up UI spacing polish для standalone bold heading paragraphs.
- `Plans/Archive/Translation_LatinCyrillic_Spacing_Architecture.md` — archived planning-doc закрытого bugfix/release scope релиза `1.2.24`; фиксирует shared text-format normalization contract для translated overlays, assistant/thinking paragraph-boundary repair и UI-side nested list whitespace collapse.
- `Plans/DevelopmentTree_BranchWorkflow_Architecture.md` — design intake для branch-level workflow сразу после `Diagram Modules`; формализует `Product Part Specification`, единые design-step'ы `Cluster Design` / `Module Design` с парой артефактов `specification + facade contract`, standalone-module path, wave-level contracts и readiness gate в `Implementation Foundation`.
- `Plans/DevelopmentTree_Sidebar_Visualization_Architecture.md` — design draft для визуализации Development Tree в левом сайдбаре PM (Product Part → Cluster → Module без artifact leaves); фиксирует PP frame, cluster connector lines, `PP`/`CL`/`M` type badges, удаление дубликата workspace root, перенос Part/Cluster/Module artifacts в tabs Artifacts panel, tooltip-on-hover requirement и открытые вопросы (Documentation Tree, custom tooltip delay, accordion mode); содержит ссылку на интерактивный prototype `doc/tmp/prototypes/development-tree-sidebar.html`.
- `Plans/Archive/Implementation_Foundation_Architecture.md` — archived planning-doc поздней branch-level фазы (Draft 2026-04-05, Accepted-статус не получен); заархивирован 2026-04-21 после ревизии active Plans: никаких следов реализации Foundation-шага в коде нет, execution-phase prioritization сместилось. Документ остаётся как reference architecture для будущего scope реализации Design/Planning/Execution agents.
- `Plans/Archive/MultiProvider_Orchestration_Scenarios.md` — archived experimental dual-agent orchestration draft ("verified manually" статус, Accepted не получен); заархивирован 2026-04-21 как out-of-scope для v1.x (нет dual-session UI, cross-broadcast буфера, session group management в коде). Потенциальный inspiration source для future multi-provider enhancements.
- `Plans/Archive/Codex_AppServer_LiveReasoning_And_SDK_Log_Architecture.md` — archived planning-doc закрытого bugfix/release scope релиза `1.2.23`; фиксирует доведение `packages/Codex_AppServer_Module` до live reasoning через app-server delta notifications, возврат file-backed transport log в `~/.codeai-hub/logs/codex` и release hygiene fix для clean `dist` build path.
- `Plans/Archive/Codex_AppServer_Module_Architecture.md` — archived planning-doc закрытого scope релиза `1.2.22`; фиксирует migration strategy от legacy Codex SDK rollout/runtime к `codex app-server` линии в `packages/Codex_AppServer_Module` при сохранении внешнего provider contract `codexCli`, provider slot `~/.codeai-hub/providers/codex` и installer artefact name `codex-module-<version>.tar.bz2`.
- `Plans/Archive/Claude_Codex_PM_FollowUp_Umbrella_1.2.19.md` — archived umbrella planning-doc закрытого scope релиза `1.2.19`; фиксирует единый phase map для Claude orphan-suffix finalization, Codex duplication pair и PM/Core event-driven telemetry + polling cleanup.
- `Plans/Archive/Claude_LiveText_OrderSafe_Finalization_1.2.19.md` — archived planning-doc закрытого Claude follow-up scope релиза `1.2.19`; фиксирует evidence и solution contract для order-safe finalization без orphan suffix assistant bubble.
- `Plans/Archive/Codex_Dialog_Duplication_StopResend_And_FinalAnswer_1.2.19.md` — archived planning-doc закрытого Codex/PM duplication scope релиза `1.2.19`; фиксирует transient user duplicate после `Stop` + fast resend и persisted final-answer duplicate из rollout terminal pair.
- `Plans/Archive/ProjectManager_MultiWorkspace_Performance_And_EventDriven_UsageRefresh_1.2.19.md` — archived planning-doc закрытого PM/Core performance scope релиза `1.2.19`; фиксирует переход на event-driven usage ownership, replay-first delivery и visibility-aware polling budget.
- `Plans/Archive/HaikuTranslation_PostReleaseBugfixes_Architecture.md` — archived planning-doc закрытого bugfix scope релиза `1.1.987`; фиксирует расследование и repair-решения по Claude Haiku translation (fail-closed engine wiring, Core-only localization path, provider-native JSONL persistence, runtime diagnostics).
- `Plans/Archive/Gemini_Stop_Abort_And_Resume_1.2.7.md` — archived planning-doc закрытого scope релиза `1.2.7`; фиксирует root cause (destructive `resetChat` на Stop + `null` requestedProviderSessionId на rebind) и решение (удалить `resetChat`, ввести provider capability `requiresPostStopResume`, сохранять pre-stop providerSessionId в `SessionProviderBindingService`, прокидывать его в `resolveProviderSessionId` → `argv.resume`).
- `Plans/Archive/Gemini_Real_Resume_And_PM_StaleSeed_Guard_1.2.8.md` — archived planning-doc закрытого scope релиза `1.2.8`; фиксирует retest-findings после 1.2.7 (argv.resume no-op, PM stale-seed `providerSessionStatus=ready` с мёртвым id, legacy SwitchRecoveryBanner toolbar) и решение (real resume pipeline через `client.resumeChat` в `gemini-session-bootstrapper.ts`, `GeminiSessionStaleBindingError` + one-shot rebind retry в `SessionRequestHandlerMessageDispatch`, удаление SwitchRecoveryBanner, nested install layout через правку `GEMINI_INSTALLER_PATHS`).
- `Plans/Archive/Gemini_InlineThoughtSplit_And_PreToolEnglishText_1.2.9.md` — archived planning-doc закрытого scope релиза `1.2.9`; фиксирует discovery после 1.2.8 retest (inline `[Thought: true]` token между english thought-summary и target-language финалом в одном `content`-потоке без `ptype: "thought"` events + english pre-tool progress-text перед первым `tool_call_request` на старте сессии с Cyrillic target) и решение (regex-splitter `/\[Thought:\s*(true|false)\]/` в `gemini-assistant-event-normalizer.ts` с routing pre-marker через `thought-translator-service` + `preToolAssistantSegment` snapshot в `TurnAccumulator` на первом `tool_call_request` с эвристикой `shouldReclassifyAsThinking` по Cyrillic-family target и U+0400..U+052F detection).
- `Plans/Archive/Audit_Cleanup_1.2.10.md` — archived planning-doc закрытого audit-cycle `1.2.10` (первый формальный периодический аудит); фиксирует 4 направления (A docs+config / B localization keys / C duplication / D process formalization), результаты (7 loc keys deleted + 3 extract refactors: `useBootstrapSettings` hook, `createWorkspaceFileHandler` factory, schema-utils consolidation через `agents/shared`; `check:dup` 2.06% → 1.97%) и новый SSOT invariant 29 об acceptable parallel-scaffolding duplication.
- `Plans/Archive/Audit_Cleanup_1.2.10_DryRun_LocKeys.md` — appendix к archived 1.2.10 cycle; полный three-pass grep-отчёт по 278 keys из 4 approved dicts (204 alive / 67 suspicious / 7 certainly-dead) с подробной breakdown'ной методологией для будущих audit cycles.
- `Plans/Archive/Gemini_InitialWatchdog_Bump_1.2.11.md` — archived planning-doc закрытого scope релиза `1.2.11`; фиксирует regression выявленную на 1.2.10 retest (Gemini 3.1 Pro + thinkingLevel=high на Description step падал через 60с с `Gemini stream stalled after 60s without progress` — deep-reasoning phase на больших prompt'ах превышает старый watchdog) и решение (bump `DEFAULT_STALLED_TURN_WATCHDOG_MS` 60_000 → 240_000 в `packages/Gemini_Module/src/session/gemini-session-lifecycle.ts`; post-tool 120_000 без изменений). Adaptive-per-thinking-level deferred на follow-up cycle если 240с окажется неподходящим.
- `Plans/Archive/Gemini_AbortCrash_And_MisroutedThinking_1.2.12.md` — archived planning-doc закрытого scope релиза `1.2.12`; фиксирует два бага, выявленных на 1.2.11 retest: (a) Core daemon crash от uncaughtException `AbortError` из `@google/gemini-cli-core` `GeminiClient.processTurn` когда cli-core сам вызывает `controller.abort()` на внутреннем loop detection (решение: `process.on("uncaughtException")` handler в `packages/core/src/index.ts` с selective suppression для `@google/gemini-cli-core` stacks); (b) mis-routed thinking content от Gemini 3.1 Pro при thinkingLevel=high — internal meta-prompt (`sthought`, `CRITICAL INSTRUCTION`, etc.) через `Content` events вместо `Thought` events (решение: `hasMisroutedThinkingPrefix` detector в `gemini-assistant-event-normalizer.ts` + reroute через существующий `emitInlineThoughtAsThinking` overlay path). Новый SSOT Invariant 30 (Provider uncaughtException safety); Invariant 7 расширен.
- `Plans/Archive/ClientLabelFallback_Fix_1.2.15.md` — archived planning-doc закрытого scope релиза `1.2.15`; companion fix к 1.2.13 (Core-side broadcast enrichment). Client-side `resolveModelReasoning` в `src/client/ui/src/session/model-info-builder.ts` возвращал raw level из settings (`"high"` / `"medium"`) при initial render до первого `session:model:update` от Core, что давало мерцание label'а между `(high)` и `(thinking high)` в один-два фрейма на старте temp-session. Fix: обернуть Gemini fallback в `thinking ${level}`, Codex fallback в `reasoning ${level}` — симметрично с `parseEffectiveModelId`. Claude ветка сохраняет свою convention. Invariant 14 расширен client-side fallback contract'ом.
- `Plans/Archive/Claude_ContextProbe_ContinuityFix_1.2.16.md` — archived planning-doc закрытого scope релиза `1.2.16`; фиксирует ложный Claude continuity lock `Agent is resuming...` после уже завершённого turn-а. Root cause split: (a) Unix `/context` probe path запускал `node <executablePath>` даже когда `claude` резолвился в native bundled executable (`claude.exe`), что ломало post-turn token usage read с `ERR_UNKNOWN_FILE_EXTENSION`; (b) shared Core post-turn arbitration не имел explicit fallback для случая, когда provider уже знает, что trailing usage snapshot не придёт. Решение: direct native runner selection on Unix unless executable is a real `.js/.cjs/.mjs` entrypoint, provider-side `postTurnTokenUsageUnavailable: true` on completed Claude turns without trailing usage, и Core `no_rollover` fallback только на этом explicit signal. Invariant 9 и `SessionContinuity` расширены.
- `Plans/Archive/Claude_LivePreToolThinking_1.2.17.md` — archived planning-doc закрытого scope релиза `1.2.17`; фиксирует Claude/UI/Translation bug, в котором localized pre-tool live text мог материализоваться как ordinary assistant bubble между двумя `Thinking` chunks и обходить translation path. Решение: narrow hold для source-language pre-tool `text_delta` в localized Cyrillic-target sessions, routing `tool_use` preambles через thinking contract, плюс regression-tests для `tool_use` vs `end_turn`.
- `Plans/Archive/Gemini_PostToolWatchdog_Bump_1.2.14.md` — archived planning-doc закрытого scope релиза `1.2.14`; фиксирует регрессию выявленную на 1.2.13 retest — `DEFAULT_POST_TOOL_STALLED_TURN_WATCHDOG_MS = 120_000` резал легитимный post-tool reasoning turn Gemini 3.1 Pro + thinkingLevel=high ровно на 120с после двух успешных `read_file` tool_call'ов. Решение: bump 120_000 → 240_000 в `packages/Gemini_Module/src/session/gemini-session-lifecycle.ts`, симметрично с initial-leg watchdog. Adaptive-per-thinking-level по-прежнему deferred.
- `Plans/Archive/ModelLabel_FlickerFix_1.2.13.md` — archived planning-doc закрытого scope релиза `1.2.13`; фиксирует cosmetic bug, выявленный на 1.2.12 retest — UI label в session status panel мерцал между `Gemini 3.1 Pro Preview (thinking high)` и `Gemini 3.1 Pro Preview (high)` внутри одного turn'а. Root cause: два broadcast-пути `session:model:update` (raw SDK `model_info` vs applied-turn-config dispatch) отправляли modelId в разных формах (`gemini-3.1-pro-preview` vs `gemini-3.1-pro-preview thinking:high`), UI renderer парсил их по-разному. Решение: обогатить SDK-путь через новый `SessionRequestHandlerAppliedTurnConfig.resolveEffectiveModelId(providerId, targetModelId)` — оба пути теперь эмитят одну effective identity. Invariant 14 (Effective model identity SSOT) расширен broadcast-contract'ом.
- `Plans/Archive.zip` + `Plans/Archive.README.md` — сжатый архив исторических planning-документов (Session023+ rollback волны Diagram Modules autolayout, Foundation Envelope история, локализационные волны, PM hotfixes `1.1.899–1.1.916`, рефакторинг рантайма, Sidecar v2 и projection rename из Session025, закрытый provider-override scope `StageConfirmationCard_Architecture.md` из релиза `1.1.972`, закрытый thinking-translation overlay scope `ThinkingTranslationOverlay_Architecture.md` из релиза `1.1.973`, закрытый translation-engine selector scope `Localization_TranslationEngine_CodexModels_Architecture.md` из релиза `1.1.975`, закрытый Codex release-hotfix scope `Codex_TranslationEngine_ReleaseHotfix_Architecture.md` из релиза `1.1.976`, закрытый post-release regression scope `Codex_PostRelease_TranslationRegression_Architecture.md` из релиза `1.1.977`, закрытый universal chunked translation scope `Universal_ChunkedTranslation_Architecture.md` из релиза `1.1.979`, закрытый localization recovery scope `Localization_Settings_RestartHydration_Architecture.md` из релиза `1.1.980`, закрытый interface-localization batching / PM blank-screen hotfix scope `Localization_InterfaceBatching_And_PMBlankScreen_Architecture.md` из релиза `1.1.981`, закрытый Codex thinking bootstrap-path hotfix scope `Codex_ThinkingTranslation_BootstrapPath_Hotfix_Architecture.md` из релиза `1.1.983`, а также закрытый reasoning no-chunking scope `Reasoning_NoChunking_Architecture.md` из релиза `1.1.984`, закрытый incremental localization sync + emission-time thinking visibility scope `Localization_IncrementalSync_And_ThinkingVisibility_Architecture.md` из релиза `1.1.985`), изначально compressed в Phase 3 cleanup (Session025, 2026-04-09) и пополняется при закрытии каждого execution cycle. Для доступа к конкретному плану — см. инструкции в `Archive.README.md`. Git history для каждого архивного документа сохранена и доступна через `git log --all --follow`.

## 2) Runtime templates (Description)

Каноничные bundled-шаблоны для шага `Description` (source-пути внутри `BUNDLED_TEMPLATE_SOURCES` в `packages/core/src/templates/bundled-templates.ts`; материализуются `template-sync-service` в `~/.codeai-hub/templates/description/` при старте):
- `.codeai-hub/templates/description/questionnaire-template.md` — анкета pre-submit.
- `.codeai-hub/templates/description/description-template.md` — user-facing Help (pre-submit и post-submit).
- `.codeai-hub/templates/description/description-collector-prompt.md` — инструкции Description Agent (file-first).

Per-workspace instances шага `Description` (создаются при открытии workflow, не bundled):
- `.codeai-hub/codeai-hub/description/questionnaire.md` — копия анкеты для конкретного workspace.
- `.codeai-hub/codeai-hub/description/description-step.json` — state шага (paths, timestamps, workspaceSlug).

## 3) Правило миграции

- Новые правки делаем только в SSOT-файлах этого каталога.
- Новый scope сначала описываем в `Plans/`, и только после реализации переносим итоговый SSOT в `System/`, `Clusters/`, `Modules/` или `Contracts/`.
- Legacy документы используем как редиректы/compat notes, но не описываем в них поддерживаемые product-возможности, уже снятые из живого кода.
