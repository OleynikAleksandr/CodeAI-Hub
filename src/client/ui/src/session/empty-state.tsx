const EmptyState = () => (
  <div className="session-empty">
    <h2 className="session-empty__title">Create your first session</h2>
    <p className="session-empty__description">
      Use the buttons above to start a session. Select one or more providers in
      the picker to begin orchestrating them together.
    </p>
  </div>
);

export default EmptyState;
